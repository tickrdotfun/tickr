// Generated by scripts/build-docs-bundle.mjs. Do not edit: the source is the repository's docs/ folder.
export const DOC_FILES: Record<string, string> = {
  "01-first-principles.md": "# 01 · Overview\n\nEverything below is a property of the contracts, not a promise. Each one names the code that makes it true.\n\n## 1. No custody by the operator\n\nNon-custodial for the operator: the pools, the locked positions, the escrow and the name wrappers hold assets by code, and nobody at tickr can move them. The site never holds funds or keys. Every transaction is built in the browser, shown, and signed in the user's own wallet. On chain, the only places money sits are the coin's Uniswap v4 pool (owned by nobody), the locked position that holds it (owned by `LaunchLocker`, which has no function to move it), the fee escrow (`FeeEscrow`, pull only: a recipient claims its own balance and nothing else moves), and a redeemable name's USDG reserve (`TickerToken`, redeemable one for one by whoever holds the wrapper). A fixed-inventory name holds no reserve at all: its supply sits in its own pool and there is nothing to redeem.\n\n## 2. Atomic flows\n\nA launch is one transaction. `Factory.launchToken` deploys the coin, opens its pool at the opening price, puts the entire supply into one locked position, and records the terms. Either all of it happens or none of it does. There is no phase between \"created\" and \"tradable\": the pool exists in the block the coin does, and a buy in that same transaction, the dev buy, is the only buy guaranteed to be first.\n\n## 3. Everything readable on-chain\n\n`Factory.getLaunchedToken(token)` returns the launch record: the pair, the opening quote reserve, the pool fee, the position's range and liquidity, the position id, the creator's tax and fee wallet. `poolKeyOf` and `poolIdOf` give the pool. `LaunchLocker.pendingFees` gives what the position has earned and not yet collected. `TickerLauncher` lists every ticker and every coin under it. Nothing needs tickr's servers.\n\n## 4. Immutable per version\n\nA coin's name, symbol, image, description, socials, pair, opening economics, pool fee and creator tax are fixed in the launch transaction. The pool key is derived from them and the fee split is copied into the launch record, so `setFeePolicy`, `setLaunchConfig` and `setFeeClub` change future launches only. A new version of the protocol is a new factory.\n\n## 5. Creator holds 0 launch tokens at creation\n\n`LaunchDeployer.deployToken` mints the supply and hands all of it to the factory, which puts all of it into the locked position. The creator receives nothing at creation. The one exception is the dev buy in the launch transaction, which buys from the pool at the opening price like anyone would, and is disclosed in the launch's own events. `Token` has no mint, no burn hook, no freeze, no blacklist, and one tax only: a buy in its first five seconds pays a snipe tax that starts at 99% and is gone by the fifth second, burned; see [02](./02-lifecycle.md).\n\n## 6. Nothing to graduate, nothing to squat\n\nNothing sits between a buyer and the pool: no curve contract in front of it, no hand-off to a pool later. The position that holds the supply spans from the opening price to the end of the range, which is exactly a constant product curve with a virtual quote reserve (see [03 pool math](./03-curve-math.md)), and it is in the pool from the first block. Because the pool is created in the same transaction as the coin, nobody can create it first at a price of their choosing.\n\n## 7. Every coin's address ends in 6942\n\nNothing on chain enforces this. The factory deploys each coin with CREATE2 at `keccak256(initiator ++ TokenParams.salt)`, and the salt is a free field, so the create page tries salts until `LaunchDeployer.predictToken` returns an address ending in `6942`. It costs no extra gas and needs no call to the chain. A coin launched through another interface with a random salt is just as valid, it only lacks the suffix.\n",
  "02-lifecycle.md": "# 02 · What a launch does\n\nA coin on tickr has one state: live. What follows is what happens in the launch transaction, what happens on every trade after it, and what happens to the fees.\n\n## 1. Create\n\nThe creator picks a name, a symbol, an image, a description, socials, a pair, an optional creator tax (0 to 2%), an optional dev buy, and a fee wallet (defaults to the launching wallet). The pair is ETH, USDG, an official Stock Token from the Robinhood Assets registry, a coin already launched here, or a name somebody invented. A name is one of two kinds, and the create page now makes only the second: a redeemable name, a one-for-one wrapper of USDG, or a fixed-inventory name with its own market and no backing. See [05 anchors](./05-anchors.md). Under a fixed-inventory name the base fee is a frozen 82 bps and the creator tax is zero, so that choice is not offered.\n\n`Factory._launch`, called through `launchToken` (ETH and USDG), `launchTokenFor` (the launch and buy router) or `launchTokenWithPair` (the ticker, Stock Token and coin launchers, which supply the pair's economics), does the following in order. Either all of it happens or none of it does.\n\n1. Checks: the launch config exists and is enabled, launching is open or the caller is whitelisted, `msg.value` equals the launch fee, the creator tax is within the cap, the symbol and name are not reserved for official assets, `expectedEconomics` matches the economics in force, the pair's decimals match.\n2. `LaunchDeployer.deployToken` deploys the coin at its CREATE2 address and hands the supply to the factory.\n3. The factory builds the pool key: the two tokens sorted by address, fee `(baseFeeBps + creatorTaxBps) * 100` in pips, tick spacing 10, no hook. Under a fixed-inventory name that arithmetic gives 8200, and `MarketTickerLauncher` refuses any config that would give anything else.\n4. `LaunchSeeder.seedLaunch` initializes the pool at the opening price and mints one position holding the entire supply, from that price to the end of the range, to `LaunchLocker`. Rounding dust of the coin goes to the locker too.\n5. The launch record and the fee policy in force are stored against the coin, the launch fee is credited to the protocol's escrow, and `TokenLaunched` and `LaunchPositionLocked` are emitted.\n\nA dev buy is a swap on the new pool in the same transaction, done by the router or launcher that called the factory: `LaunchAndBuyRouter.launchAndBuy` for ETH and USDG, `launchAndBuy` on the ticker launcher (paid in USDG, wrapped into the ticker on the way in), `launchWithStockQuoteAndBuy` and `launchWithCoinQuoteAndBuy` for the other two.\n\nInventing a new ticker costs `TickerLauncher.NEW_TICKER_FEE` on top of the launch fee. That fee buys USDG and becomes the working dollars of the ticker's own pool, which opens at one dollar in the same transaction. See [05 anchors](./05-anchors.md).\n\n## 2. Trade\n\nThe pool is a plain Uniswap v4 pool. Anything that trades Uniswap v4 on Robinhood Chain trades it from the block it is created in: the site through `ZapRouter`, `LaunchSeeder.swapExactIn` for a single pool, Uniswap's own router, aggregators, terminals and bots. The pool's LP fee is the 1% base plus the creator's tax, charged on every swap in the asset that goes in: buys pay it in the pair, sells pay it in the coin.\n\nA buy out of the pool in a coin's first five seconds pays a snipe tax: 99% in the launch second, 25% one second in, 3% at two, then dust, then nothing from the fifth second on. It is burned to the dead address, as the protocol's and the club's coin-side shares are; the creator's coin-side share and tax are not burned, they go to the creator's escrow. The launcher's wallet and its fee wallet never pay it, and sells never do. Beside it, counted in blocks: in the launch block only the launch's own wallets may buy out of the pool, and in the two blocks after it every other wallet may hold at most 5% of supply and buy at most 5.5% of it, all its buys added up, net of the tax. A buy that would break either limit reverts whole. Sells are never limited. A transfer between wallets inside those two blocks is held to the same 5%, so a holding over the cap cannot be assembled from several wallets while the window is open; it counts as nothing bought. From the fourth block every limit is gone for good and transfers are free. There is no trading delay beyond that. The dev buy is the creator's way to be first.\n\nWhat the two rules cover, exactly: coins leaving the pool manager as ERC-20 transfers, and, for the hold cap, coins arriving anywhere but the launch's own wallets and the protocol's contracts. A trader who settles a swap into ERC-6909 claim balances inside the pool manager and withdraws them after the window is seen by neither rule; the usual routers, the seeder and the zap, never do that. So the protection is a best-effort deterrent for standard routers, not a guarantee: the rules make the obvious routes expensive in the first seconds; they do not make a launch impossible to snipe, they do not see claims held inside the pool manager, and they do not stop a wallet that bought under the cap from buying again once the window closes. The site says \"launch protection is on\" while the window is open and says the same thing about what it covers.\n\n## 3. Fees\n\nA swap through `LaunchSeeder.swapExactIn` that a pool cannot fill in full returns what the pool did not take to the caller in the same transaction. Fees accrue to the locked position, like every Uniswap fee. `LaunchLocker.collectFees(token)` pulls what the position has earned and splits it by the terms frozen at launch. Anyone may call it, at any time. Details in [06 fees](./06-fees.md).\n\n## The locker\n\n`LaunchLocker` holds every launch position and every rounding remainder. It can receive positions and collect their fees, and nothing else: nothing in it can hand a position to anyone or take liquidity out of one, whoever asks, the owner and the creator included. Fee collection is a zero-liquidity decrease, which Uniswap uses to pay out owed fees without touching the principal. Other people may add liquidity to a tickr pool and remove their own again; only the launch position is locked.\n",
  "03-curve-math.md": "# 03 · Price and market cap\n\nThere is no curve contract. The launch position is the curve: one Uniswap v4 position holding the entire supply from the opening price to the end of the range. This page is what that means in numbers.\n\n## The opening price\n\nEach pair has a `phantomQuote`, call it `P`: 1.68 ETH for ETH, 3,236 USDG for USDG and redeemable names, 3,236 of the name for a fixed-inventory name, and for a Stock Token or a coin the amount its launcher derives from the live price at launch (see [04](./04-custom-pairs.md), [10](./10-coin-quotes.md), [11](./11-stock-quotes.md)). With `S` the supply, always 1,000,000,000 coins, the pool opens at\n\n```\np0 = P / S    quote per coin\n```\n\n`LaunchSeeder.seedLaunch` aligns that to the pool's tick spacing of 10, which is a step of 0.1%, and mints the position from that tick to the last usable tick with all `S` coins on the coin's side and nothing on the quote side. The opening market cap is `p0 * S = P`, the same figure for every quote asset once converted, because every launcher sizes `P` from a dollar target.\n\n## What the position does\n\nA one-sided position from `p0` upward behaves like a constant product pool whose quote side starts with a virtual balance of `P`. Ignoring the fee, with `q` the quote buyers have put in so far:\n\n```\ncoins left in the pool   t = P * S / (P + q)\nprice                    p = (P + q)^2 / (P * S)\nmarket cap               m = (P + q)^2 / P\nshare of supply sold     q / (P + q)\n```\n\nFor the ETH pair, `P = 1.68 ETH`:\n\n| quote in `q` | market cap | sold |\n| --- | --- | --- |\n| 0 | 1.68 ETH | 0% |\n| 1 ETH | 4.27 ETH | 37.3% |\n| 5 ETH | 26.6 ETH | 74.9% |\n| 20 ETH | 280 ETH | 92.3% |\n\nFor the USDG pair and every redeemable name, `P = 3,236 USDG`:\n\n| quote in `q` | market cap | sold |\n| --- | --- | --- |\n| 0 | 3,236 USDG | 0% |\n| 1,000 USDG | 5,545 USDG | 23.6% |\n| 8,090 USDG | 39,640 USDG | 71.4% |\n| 50,000 USDG | 875,900 USDG | 93.9% |\n\nUnder a fixed-inventory name the same table applies with `P = 3,236` of the name. `MarketTickerLauncher.economics()`\nhands the launch USDG's own `phantomQuote` with the name's decimals, which are also 6, so a market opens at parity\nin raw units. The number is the same; what it is worth is not. 3,236 USDG is 3,236 dollars by construction, and\n3,236 of a fixed-inventory name is 3,236 dollars only while that name happens to trade at a dollar. Read the\nopening market cap of such a coin in the name, and convert it through the name's own pool if you want dollars.\n\nNothing about this is special to tickr. It is the standard shape of a one-sided Uniswap v3 or v4 position, which is how most launchpads open a token. The table is before fees: a buy pays the LP fee on the quote it puts in and the remainder moves the price, so `q` is the quote net of fees.\n\n## Fees in the math\n\nThe pool's LP fee is `(baseFeeBps + creatorTaxBps)` in bps. On the standard path the base is 100, so 1% with no tax and up to 3% with the maximum tax. Under a fixed-inventory name the base is a frozen 82 and the creator tax is always zero, so the fee is exactly 0.82% and nothing else is possible. Uniswap takes it from the input of every swap: a buy of `x` quote moves the price with `x * (1 - fee)` and books `x * fee` of quote to the position; a sell of `y` coins moves the price with `y * (1 - fee)` and books `y * fee` of coins to the position. Those booked amounts are what `LaunchLocker.collectFees` pays out.\n\n## Reading the price\n\nSpot price lives in the pool manager's slot0 for the pool id: `price = (sqrtPriceX96 / 2^96)^2`, currency1 per currency0, raw units. The site reads it through `poolIdOf(token)` and `extsload`; [17 reading state](./17-reading-state.md) has the exact call. Market cap is that price times the supply.\n\n## Third party liquidity\n\nAnyone may add liquidity to a tickr pool at any price and remove their own again. Trades then cross whatever liquidity is in range, ours and theirs, and the LP fee is shared in proportion. The launch position is by far the largest in practice, and it is the only one that is locked.\n",
  "04-custom-pairs.md": "# 04 · ETH, USDG and Stock Tokens (Mode 1)\n\n> One of the custom pairs a launch can use. The others are a redeemable name (05), another coin (10), and a\n> Stock Token sized from its feed (11).\n\nA launch's `pairToken` is the asset its pool is paired with and priced in. It is fixed at creation and stored in `LaunchedToken.pairToken`. `address(0)` is native ETH; anything else must be an **approved** ERC-20.\n\n## What \"approved\" means\n\n```solidity\nfunction approvedPairTokens(address pairToken) external view returns (bool);\n```\n\nReturns `true` for `address(0)`. For an ERC-20 it is true iff **both**:\n\n1. `AnchorRegistry.isApproved(pairToken)`. the token is registered and active (kinds: 0 ETH, 1 USDG, 2 official Stock Token; see [05 Anchors](./05-anchors.md)), and\n2. the factory has economics on file: `pairTokenEconomics(pairToken).phantomQuote != 0`.\n\nLaunching with an unapproved ERC-20 reverts `PairTokenNotApproved`.\n\n## Per-asset economics\n\nA phantom reserve is a *quantity of the quote asset*, so it cannot be shared across assets with different prices or decimals. The `LaunchConfig` carries native-ETH economics; each ERC-20 gets its own record:\n\n```solidity\nstruct PairEconomics {\n    uint256 phantomQuote;  // in the pair's base units\n    uint8 decimals;        // read from the token when approved\n}\n\nfunction pairTokenEconomics(address pairToken)\n    external view returns (uint256 phantomQuote, uint8 decimals);\n```\n\nTwo fields, not three. Set by the owner with `setPairTokenEconomics(pairToken, phantomQuote)`; it reads\n`decimals()` from the token at that moment and emits `PairTokenApproved(pairToken, phantomQuote, decimals)`.\nPassing 0 deletes the record and emits `PairTokenRevoked`. Revoking affects new launches only.\n\nExample, USDG (6 decimals), read from the live factory:\n\n| Field | Value |\n|---|---|\n| `phantomQuote` | `3_236e6` (3,236 USDG) |\n| `decimals` | 6 |\n\nUSDG is registered in `AnchorRegistry` as kind 1 (issuer \"Global Dollar Network\").\n\nFor a native launch, `_resolveEconomics` returns `PairEconomics(config.phantomQuote, 18)`, so ETH takes its\nphantom from the launch config rather than from a pair record. A coin launched under a fixed-inventory name takes\n**USDG's** record with the name's own decimals, which are also 6: see\n[05 anchors](./05-anchors.md) and [03 price and market cap](./03-curve-math.md) for what that means for the\nopening figure.\n\n## Decimals check\n\nThe factory reads `IERC20Metadata(pairToken).decimals()` at launch. If it differs from the recorded value the launch reverts `PairTokenDecimalsMismatch`. This protects against a pair whose decimals changed (or were misread) between approval and launch. `Curve.pairDecimals()` exposes the value for UIs.\n\n## Pinning economics: `expectedEconomics`\n\nEvery economic term of a launch is hashed and must be passed back in `TokenParams.expectedEconomics`, so a launch can never settle on terms you did not read:\n\n```solidity\nfunction previewLaunchEconomics(uint256 launchConfigId, address pairToken) external view returns (bytes32);\nfunction previewLaunchEconomicsWithPair(uint256 launchConfigId, address pairToken, PairEconomics calldata econ)\n    external view returns (bytes32); // registrar path (coin, stock and ticker launchers)\n```\n\nThe hash, exactly as `Factory._economicsHash` computes it:\n\n```solidity\nkeccak256(abi.encode(\n    launchConfigId, config.supply, config.baseFeeBps,\n    econ.phantomQuote, econ.decimals, config.tickSpacing, pairToken,\n    policy.creatorShareBps, policy.clubShareBps, policy.protocolShareBps,\n    policy.protocolFeeRecipient, policy.buybackBurnBps,\n    feeClub, launchFee\n))\n```\n\n`policy` is the factory's `defaultPolicy` as it stands, **not** the resolved split: a name invented inside the\nlaunch has no club at preview time and one at launch time, so the pin has to agree with itself across that. The\npool fee is not in the hash; `config.baseFeeBps` and your own `creatorTaxBps` determine it.\n\nDo not hand-roll this. Call `previewLaunchEconomics` (or `previewLaunchEconomicsWithPair` on a registrar path) and\npass back what it returns. The pin covers the fee split, the protocol recipient, the club and the launch fee as\nwell as the curve, so if the owner edits any of them between your read and your transaction the hash changes and\n`_launch` reverts `LaunchEconomicsMismatch`. Read it in the same block-height context as you build the\ntransaction.\n\n## Calling conventions\n\n| | Native (`pairToken == address(0)`) | ERC-20 pair |\n|---|---|---|\n| `Factory.launchToken` | `value = launchFee()` | `value = launchFee()` |\n| `LaunchAndBuyRouter.launchAndBuy` | `value = launchFee() + quoteIn` | `approve(router, quoteIn)`; `value = launchFee()` |\n| Refunds on fill-to-edge | ETH sent back to `msg.sender` | ERC-20 sent back to `msg.sender` |\n| Fees at sweep | `FeeEscrow.credit{value}` → `claim()` | `FeeEscrow.creditToken` → `claimToken(pairToken)` |\n| The pool | `currency0 = address(0)` (ETH is always currency0) | currencies sorted by address |\n\nThe router refunds any leftover value or ERC-20 balance to `msg.sender` after the first buy.\n\n## Risks of quote-asset exposure\n\n- Registering an asset in `AnchorRegistry` is a suitability judgement, not an endorsement. Deactivating an anchor affects only new launches; existing curves keep trading in it.\n- Redeemable names and other coins (see [05 Anchors](./05-anchors.md)) are never globally approved: `approvedPairTokens(quote)` stays `false`. They are admitted for one launch at a time through a registrar, with economics read from their live pool.\n",
  "05-anchors.md": "# 05 · Anchors: official assets and invented names\n\n> On the create page this is the \"invent a name\" option: the name is the quote asset your coin is priced in, the second half of the pair, not your coin's own ticker.\n\nA launch is priced in whatever it is quoted in. That quote is either an **official anchor** (ETH, USDG, an official Stock Token) or an **invented name**: a token somebody named on tickr.\n\nThere are two kinds of invented name, and they behave differently.\n\n- A **redeemable name** is a one-for-one wrapper of USDG. One BANANA is worth one dollar because the contract holds the dollar. Every name made before the market release is this kind, FUN included.\n- A **fixed-inventory name** has 500,000,000 of itself and nothing behind it. It is worth whatever its own market says. This is what the create page makes now.\n\nWhich kind a name is, is decided when it is created and never changes. `QuoteRegistry.kindOf(name)` answers it by\nprovenance, from which issuer made the name, so nothing has to trust a name's own answer about itself.\n\n> BANANA and BREAD run through this page as one worked example, and that is all they are: two names somebody typed. There is no official name and no reserved list beyond the anchors below. Any name that is free is yours to use.\n\n|  | Official anchor | Redeemable name | Fixed-inventory name |\n| --- | --- | --- | --- |\n| Quote asset | ETH, USDG, or an official Stock Token issued by Robinhood Assets, listed in `AnchorRegistry` | A `ManagedTickerToken`: 1 BANANA is redeemable for exactly 1 USDG, mint and redeem one for one between transactions (while the pool manager is locked), and the name runs its own pool at a dollar | A fixed supply of 500,000,000 at 6 decimals in its own USDG pool. No mint, no redeem, no backing |\n| Entry point | `Factory.launchToken` / `LaunchAndBuyRouter.launchAndBuy` | `TickerLauncher.launch(symbol, ...)`: existing name or brand new, same call | `MarketTickerLauncher`: existing name or brand new, same call |\n| Pair approval | Global (`approvedPairTokens == true`) | Never global. Each coin under it is priced in it through the registrar path, on USDG's economics | Never global. Same registrar path |\n| Issuer | Robinhood Assets for Stock Tokens; native / stablecoin issuer otherwise | Nobody. The wrapper has no owner and no admin function | Nobody. `MarketTickerDeployer` issues it and holds no power over it after |\n| Who holds the name | n/a | Whoever minted some and has not redeemed it yet. Nobody at birth | Whoever bought some. The whole supply starts in the name's own pool |\n| What it is worth | Its market | Exactly its counter asset, by construction | Its market, and nothing guarantees a floor |\n| UI | Official badge | `YOURCOIN/BANANA` with the badge **creator-issued** | `YOURCOIN/BANANA` with the badge **creator-issued** |\n\n## AnchorRegistry\n\n```\nstruct Anchor { string ticker; string issuer; uint8 kind; bool active; }\n\nuint8 KIND_NATIVE = 0;          // ETH, registered at address(0) in the constructor\nuint8 KIND_STABLE = 1;          // USDG\nuint8 KIND_OFFICIAL_STOCK = 2;  // official Stock Token at its canonical address\n\nfunction register(address token, string ticker, string issuer, uint8 kind, address feed) external onlyOwner;\nfunction setActive(address token, bool active) external onlyOwner;\nfunction reserveTicker(string ticker, bool reserved) external onlyOwner;\nfunction isApproved(address token) external view returns (bool);   // active anchor\nfunction isReservedTicker(string ticker) external view returns (bool);\n```\n\nThe registry is the list of real assets. It answers two questions: *is this a real anchor?* (`isApproved`) and *is this symbol spoken for?* (`isReservedTicker`). Every anchor's ticker is reserved, so an invented name cannot be called NVDA, USDG or ETH.\n\n## What a redeemable name is\n\nA one-for-one wrapper of USDG. Every name invented before the market release is one of these, and no name made after it is.\n\n```\ncontract ManagedTickerToken is ERC20 {\n    IERC20 public immutable counter;                        // USDG\n    function decimals() public view returns (uint8);        // the counter's, so one for one is literal\n    function mint(uint256 amount, address to) external;     // amount USDG in, amount BANANA out; only while the pool manager is locked\n    function redeem(uint256 amount, address to) external;   // amount BANANA burned, amount USDG out; the same condition\n    function accounting() external view returns (uint256 backing, uint256 circulation); // backing >= circulation, always\n    function poolKey() external view returns (PoolKey);     // its own pool against USDG, described below\n}\n```\n\nNo owner, no fee on mint or redeem, no pause, no blacklist, no upgrade. Two kinds of BANANA exist. What is in circulation, everything a wallet or a pool other than the name's own holds, exists only because a dollar was paid for it: deposited through `mint`, or paid into the name's own pool by a buyer. That dollar is its backing, and the only way it leaves is against the burn of that BANANA, or against a sale of it back into the pool. The other kind is the wrapper's own inventory: BANANA it mints to itself and keeps in its own pool positions as the offer to buyers. Inventory is owed to nobody and backed by nothing, it is never in circulation, and it becomes circulation only at the moment a buyer pays for it. So `totalSupply()` counts both, and the number that means something is `circulatingSupply()`; the rule the contract enforces before and after every operation is `backing >= circulation`, not a comparison with the total supply.\n\nIt is not a stablecoin in the sense that ever goes wrong. Nothing maintains a price on other venues: what the contract gives is convertibility one for one with the USDG it holds, plus a market of its own that it keeps at a dollar. A name is redeemable for USDG; it is not a promise of a US dollar.\n\n## Why the price holds\n\nA redeemable name's value is not defended on someone else's market, and it is not discovered by one. It is defined by the contract, in two ways that back each other up.\n\n<figure class=\"doc-fig\"><!-- alt:\n                    mint\n    1 USDG  ==================>  1 BANANA\n    1 USDG  <==================  1 BANANA\n                   redeem\n    one for one, no fee, while the pool manager is locked\n-->\n<svg viewBox=\"0 0 680 172\" role=\"img\" aria-label=\"One USDG can be minted into one BANANA, and one BANANA redeemed back into one USDG, at a one to one rate with no fee, outside any route\"><defs><marker id=\"fig-a\" viewBox=\"0 0 8 8\" refX=\"7\" refY=\"4\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0L8 4L0 8z\" fill=\"currentColor\"/></marker></defs><g fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\"><path d=\"M180 66H500\" marker-end=\"url(#fig-a)\"/><path d=\"M500 106H180\" marker-end=\"url(#fig-a)\"/></g><g font-family=\"ui-monospace, monospace\" font-size=\"14\" fill=\"currentColor\"><text x=\"60\" y=\"70\">1 USDG</text><text x=\"60\" y=\"110\">1 USDG</text><text x=\"520\" y=\"70\">1 BANANA</text><text x=\"520\" y=\"110\">1 BANANA</text><text x=\"320\" y=\"52\" text-anchor=\"middle\">mint</text><text x=\"320\" y=\"130\" text-anchor=\"middle\">redeem</text><text x=\"340\" y=\"160\" text-anchor=\"middle\" font-size=\"12\">one for one, no fee, while the pool manager is locked</text></g></svg>\n</figure>\n\nA name is one to twelve ASCII letters or digits, case-folded to upper case; nothing else, so no name can look like NVDA, USDG or ETH through another script. A coin's own symbol follows the same rule, and a coin's name is printable ASCII: letters, digits, punctuation and single spaces, none at either end.\n\nTwo consequences follow, and together they are the whole argument:\n\n- **No ceiling to break.** If a ticker ever traded above a dollar somewhere, anyone could mint at one for one and sell into that price. Minting is open to every address, so the supply available to meet a premium is bounded only by the dollars people bring.\n- **No floor to fall through.** If it ever traded below a dollar somewhere, anyone could buy there and redeem for the full amount. The wrapper can always pay, because backing covers circulation.\n\n<figure class=\"doc-fig\"><!-- alt:\n   above $1 elsewhere: anyone mints at a dollar and sells\n                        |\n                        v\n   $1.00 =====================================\n                        ^\n                        |\n   below $1 elsewhere: anyone buys it and redeems for a dollar\n-->\n<svg viewBox=\"0 0 680 196\" role=\"img\" aria-label=\"Above a dollar elsewhere anyone mints at a dollar and sells; below it anyone buys and redeems for a dollar. Both push the price back to the counter asset.\"><defs><marker id=\"fig-b\" viewBox=\"0 0 8 8\" refX=\"7\" refY=\"4\" markerWidth=\"7\" markerHeight=\"7\" orient=\"auto-start-reverse\"><path d=\"M0 0L8 4L0 8z\" fill=\"currentColor\"/></marker></defs><g fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\"><path d=\"M60 98H620\"/><path d=\"M340 40V86\" marker-end=\"url(#fig-b)\"/><path d=\"M340 156V110\" marker-end=\"url(#fig-b)\"/></g><g font-family=\"ui-monospace, monospace\" font-size=\"13\" fill=\"currentColor\"><text x=\"340\" y=\"30\" text-anchor=\"middle\">above $1 elsewhere: anyone mints at a dollar and sells</text><text x=\"340\" y=\"180\" text-anchor=\"middle\">below $1 elsewhere: anyone buys it and redeems for a dollar</text><text x=\"30\" y=\"102\">$1.00</text></g></svg>\n</figure>\n\nBoth trades are open to every address on the chain, permissionlessly, while the pool manager is locked, which is between transactions, so not from inside a route that is already trading. They are not free of risk: the other venue's price, its fees and its depth are that venue's, and a route through the name's own pool pays that pool's fee, spread and rounding, is bounded by its offer, and may visit it once per transaction. Neither trade depends on tickr, on a market maker, or on anyone's good behaviour.\n\nThe name's own pool, described further down, is the second way: the wrapper keeps it at a dollar itself, so the first way is rarely needed.\n\nThis is what \"only the coin is speculative\" means in the contracts rather than in the copy: the pair is defined, and the coin is discovered.\n\n## What a fixed-inventory name is\n\nThe kind the create page makes now. There is no wrapper, no counter asset held, and no redeem.\n\n```\nsupply    500,000,000 at 6 decimals, minted once, at birth\npool      the name against USDG, no hook: fee 500, tick spacing 10, width 30\ninventory the whole supply opens in that pool, on the sell side\nmint      none after birth\nredeem    none, ever\nbacking   none\n```\n\nEvery one of these names runs its own USDG market, and the price of the name is whatever that market says. Buying\nthe name moves it up, selling it moves it down. Nothing defends a dollar, because nothing is holding a dollar.\n\nThat is the whole difference, and it is the point. A redeemable name is a receipt for a dollar, so a coin priced\nin one is really priced in dollars wearing a name. A fixed-inventory name is an asset in its own right, so a coin\npriced in one rises and falls against that name, not against the dollar. `YOURCOIN/BANANA` becomes a real pair.\n\nWhat it costs you, said plainly: a coin priced in a fixed-inventory name has no floor underneath its quote asset.\nIf the name falls, the coin's dollar value falls with it even when the coin held its price in the name. A\nredeemable name cannot do that. Neither behaviour is safer than the other in general, they are different trades,\nand the create page lets you pick.\n\nThe name's pool carries no hook. A redeemable name's pool is managed by its wrapper through `ManagedTickerHook`,\nwhich tops up the offer and re-centres at a dollar. A fixed-inventory name has nothing to re-centre to, so there is\nnothing to manage and no hook to run.\n\n### Telling the two apart\n\n`QuoteRegistry.kindOf(name)` classifies by provenance: which issuer deployed the name, not what the name claims.\n\n```\n0  unknown          not issued by either launcher, or older than the registry\n1  redeemable       issued by TickerLauncher, a one-for-one USDG wrapper\n2  fixed inventory  issued by MarketTickerDeployer, its own market\n```\n\nKind `0` matters. FUN, the name TICKR is priced in, predates the registry and reads `0`. Everything that has to\nknow treats an unclassified name the way it always did, as a redeemable wrapper, because that is what it is.\n\n## A coin under a ticker\n\nA coin priced in a redeemable name is an ordinary launch whose pair is the wrapper. `TickerLauncher.launch(symbol, coin, launchConfigId)` creates the ticker if it is new and calls `Factory.launchTokenWithPair` with the USDG economics: the pool opens at 3,236 of the wrapper per billion coins, the entire supply locked in it from the first block. `launchAndBuy(symbol, coin, launchConfigId, usdgIn, minTokensOut)` does the same and then the creator's first buy: `usdgIn` USDG is pulled from the caller, wrapped one for one, and spent in the new pool. That first buy is paid in USDG the caller already holds, so from the site it is one approval and one launch, then the two listing buys: nothing here promises the reference's exact count of signatures for every mode, only its order.\n\nBuying such a coin from the site is one transaction paid in ETH or USDG: the zap turns ETH into USDG through the live pool, turns that into the ticker, through the ticker's own pool or by wrapping one for one, whichever gives more, and buys from the coin's pool. Selling runs the same route backwards and ends in USDG or ETH. Nobody has to hold the wrapper.\n\nEvery coin under a ticker sorts below it: the coin is currency0 of its pool and the name currency1, so the base and the quote read the same way on every chart. `TickerLauncher` places every name in the top sixteenth of the address space, and a launch whose coin would sort above its name reverts with `CoinNotFirst`; the site grinds the coin's salt for it, like it does for the `6942` suffix.\n\n## Cost\n\nLaunching under a ticker that exists costs the launch fee, 0.0005 ETH. Inventing a new ticker costs `TickerLauncher.NEW_TICKER_FEE`, 0.0015 ETH, on top of it. That fee is not kept: it is converted to USDG in the same transaction and becomes the working dollars of the ticker's own pool, described below.\n\n## Nobody owns a ticker\n\nThere is no owner slot, no role, and no line in any later pair's fee split for whoever typed the name first. Opening BANANA with BREAD grants BREAD its own 50% and one thing in the club: the founder's coin is the club's captain by default, and the captain counts double when a pot is split. That is a weight, not a right. The founder cannot touch any other coin's fees, cannot keep anyone out, and holds nothing over the name. `TickerLauncher` itself has no owner either.\n\n## The ticker club\n\n\n- The club's 10% of the quote side, what buys pay, is booked per paying coin into `pot(ticker, window, coin)`, in the window the fee is swept, and held by the launcher in the ticker itself; the coin side's club share is burned by the locker and never pooled. Said plainly: a pot lands in the window of the sweep, not of the trades behind it. Anyone may sweep at any time, so a sweep just after a window closes books those fees to the new window. Volumes, which decide the weights, are booked the same way: when a coin's fees are collected, the locker books the quote fees collected divided by the pool fee rate as that coin's volume for the window of the collection. so it stands for buy volume in the quote, since only buys pay fees in the quote, and a coin's weight for a window depends on when someone collected.\n- A coin never pays its own creator through the club. Its share of another coin's pot is `pot × its weight / the total weight under the ticker`, the payer's weight included. A coin's weight is its booked window volume, and the captain's is twice its volume, so the total weight is the window's volume plus the captain's volume once more. The payer's own share of its pot goes to the protocol through `sweepDeadPot`, so a coin with dust volume earns dust, and a coin with no volume in the window earns nothing.\n- Every club has a captain, decided per window from that window's final booked volumes, so from what was collected in the window, not from trades nobody collected for. The founder's coin, the first launched under the ticker, is captain whenever it has any volume in the window; there is no floor. In a window where it has none, the coin with the most volume under the ticker is captain instead (`topOf`, kept as volume lands, ties to the incumbent), and the founder is captain again in any later window it trades in. Worked example: BREAD founded BANANA, PEEL and CHIP joined, and in one window each of the three traded 1,000 BANANA. Weights are 2,000 for BREAD, 1,000 for PEEL and 1,000 for CHIP, 4,000 in all: of CHIP's pot, BREAD's creator claims a half, PEEL's a quarter, and CHIP's own quarter goes to the protocol. Had BREAD not traded that window, PEEL and CHIP would have tied at 1,000 and the first to record volume would have kept the seat. The seat is a weight and nothing more: the captain has no say over any coin and no access to any fee but its own share. A pot is booked to the window the collection happens in, not the window the trades happened in: a collection held back past a window boundary lands in the next window's pot, with that window's weights and captain. Anyone may call `collectFees` at any time, so a coin that wants its fees booked where they were earned has only to collect before the window closes. Nothing collects on a schedule any more: the keeper that used to is switched off, so a coin that does not collect for itself is not collected for. Claims and the protocol sweep pay by amount, so a pot that grows after a claim is still claimable for the rest.\n- No volume in the window, no share. A pot with nobody to go to (no other coin traded that window) goes to the protocol, `sweepDeadPot`.\n- Claims open when the window closes, so nothing about a claim can move after it becomes claimable. Anyone may call; the money goes to the member's current fee recipient.\n\n```\nfunction claimClub(address member, address[] payers, uint256 epoch) returns (uint256);   // closed epochs only\nfunction claimable(address member, address[] payers, uint256 epoch) view returns (uint256);\nfunction sweepDeadPot(address token, uint256 epoch) returns (uint256);                   // to the protocol\nfunction pairsOf(address ticker) view returns (address[]);\nfunction captainOf(address ticker, uint256 epoch) view returns (address);                 // the founder's coin while it trades, else the top coin\nfunction topOf(address ticker, uint256 epoch) view returns (address);                     // the most booked volume this epoch, kept as collections land\nfunction volumeOf(address token, uint256 epoch) view returns (uint256);                  // booked at fee collection: quote fees collected / pool fee rate, so buy volume in the quote\nfunction clubVolume(address ticker, uint256 epoch) view returns (uint256);\nfunction pot(address ticker, uint256 epoch, address token) view returns (uint256);\nfunction currentEpoch() view returns (uint256);                                          // block.timestamp / 30 days\n```\n\n\nOutside a ticker there is no club, so an official pair freezes 60% creator, 40% protocol.\n\n## Reserved tickers\n\n`TickerLauncher` refuses any symbol the registry reserves (`TickerReserved`), and so does the factory for the coin itself. USDG, ETH and every official Stock Token symbol are reserved at deploy.\n\n## One ticker per symbol\n\nSymbols are claimed once, case-insensitively: BANANA, banana and Banana are one ticker at one address. A second launch that names it simply joins it. The create page checks `tickerFor` as you type and shows who is already there.\n\n## Required disclosure\n\nShown verbatim on the docs index and the terms page:\n\n> Not issued by Robinhood Assets. Not a Stock Token. No mint/redeem against listed shares. A name invented on tickr is not the asset it is named after: a redeemable name is worth exactly the USDG it wraps, a fixed-inventory name has no backing and is worth only what its own market says.\n\n## What it inherits\n\nA ticker is exactly as safe as the dollar it wraps. If USDG depegs, freezes, or blacklists the wrapper's address, the ticker follows. That is the same risk as pairing against USDG directly, which every launch on this chain already carries; tickers do not add to it.\n\n## Depth one only\n\nA coin priced in a ticker cannot itself be a quote. `CoinQuoteLauncher.quotePrice` requires the quote coin's pair to be an approved anchor, and a ticker is a wrapper, not an anchor. So every coin on tickr is at most one hop from a real asset: `COIN → TICKER → USDG`, never a tower.\n\n## The name's own pool\n\nA ticker is redeemable one for one for the USDG it wraps, not a promise of a US dollar, and a name with no market is a name no chart can price and no router can reach. So every redeemable name runs its own Uniswap v4 pool against USDG from the moment it is invented, at a fixed 0.05% fee, behind one hook shared by every one of them, `ManagedTickerHook`. A fixed-inventory name runs its own USDG pool too, at the same 0.05% fee but with tick spacing 10 and no hook, because it has no dollar to be re-centred on. The pool is an ordinary pool with ordinary swaps, no custom accounting and no dynamic fee, so any router that trades Uniswap v4 can trade it; whether a third party routes through it, lists it, or shows it the right way round is that party's choice, not something the contracts can promise. The hook only hands each pool's callbacks to the wrapper that owns it.\n\nWhat the wrapper does with them. Its unsold inventory is the buy side: tokens it mints to itself, owed to nobody and backed by nothing until somebody buys them, when the dollars paid become their backing. At rest that offer is `INVENTORY_FLOOR`, ten thousand dollars' worth (the value the reference traded with; not ten thousand dollars deposited anywhere), and it grows by four times whatever is in circulation, so one buy through the pool can take that much and fills whole or not at all; the site wraps one for one for anything larger. The wrapper's backing is the sell side: every dollar anyone paid in, on offer at a dollar each, so whatever is in circulation can always come back. Before every outside swap the wrapper rebuilds both positions and re-centres the price at exactly one dollar, paying for the tiny re-centring swap from its own surplus, never from the backing; after the swap it checks that the fill was whole, that the price is within two ticks of a dollar, and that the backing still covers the circulation. If any of that fails the swap reverts and nothing moved. One visit to the pool per transaction; a route that crosses the same name's pool twice reverts.\n\nThe surplus that pays for re-centring is the ticker fee, converted to USDG in the launch transaction and handed to the wrapper as a donation that nobody can redeem, plus the pool's own fees as they accrue. Backing covers circulation before and after every operation, and `mint` and `redeem` still work one for one at any time (while the pool manager is locked, so not from inside a route), so the pool cannot be pushed off a dollar at any size: a buy above the offer or a sell above the circulation reverts whole. Ordinary swaps in the pool carry the pool's fee, spread and rounding; they are not the fee-free exact mint and redeem. The name's positions are managed by the wrapper and rebuilt at every swap; they are not the locked launch position of a coin, and are not described as one. Only the wrapper adds or removes liquidity in its pool, and only while it is maintaining; the pool holds no position of anyone else's. Anyone can open a second WRAPPER/USDG pool without the hook, at any price, because Uniswap pools are permissionless; that pool is theirs, not the ticker's, and every trade in it away from a dollar hands an arbitrage to whoever mints or redeems the wrapper.\n\n## Listing: the two buys after a launch\n\nChart sites and trackers priced the reference launch's name from a swap that landed in a wallet after the name's pool existed, and its coin from a buy in a transaction after the launch. The launch transaction itself, first buy included, counted for neither. So a launch under a name is not finished until two more transactions land, one after the other, both sent through Uniswap's canonical Universal Router with an explicit path of the launch's own pool keys, exactly as the reference sent them:\n\n1. `0.0005 ETH` of the name, through the ETH/USDG pool and the name's own pool, the name delivered to the creator's wallet.\n2. After that receipt is confirmed and a later block exists: `0.001 ETH` of the coin with fresh ETH, through the same two pools and the coin's own, the coin delivered to the same wallet. The name bought in the first stays in the wallet.\n\nBoth stages, every time, for a fresh name and for an existing one; skipping the first is an untested shortcut. Each is a separate wallet signature with its own fresh quote from Uniswap's quoter, a minimum one percent under it that is never zero, an explicit gas and fee cap, and its own cost ceiling; a quote that cannot be read stops the step, it never lowers the protection. The site keeps a record of each attempt in the browser before the wallet opens, records the transaction hash the moment the wallet returns it, waits for pending transactions rather than resending, and asks for the hash by hand when a wallet's answer was lost. It shows a coin as listed only when both receipts are canonical, in order, and show the router's swaps on exactly those pools and the delivery to the wallet. The amounts are the reference's test parameters, not proven minimums or dollar values; other amounts need their own acceptance test.\n\nBoth buys landing is not the same as showing up on a chart: chart sites and aggregators index on their own clock and by their own rules, the reference's name showed up minutes later, and a price on a chart is not a trade. tickr does not promise a place on any chart, a market of any size, or a market capitalisation. The coin trades on tickr either way.\n",
  "06-fees.md": "# 06 · Fees\n\nThree inputs, one place they land, one function that splits them.\n\n## The inputs\n\n| what | amount | when |\n| --- | --- | --- |\n| launch fee | 0.0005 ETH | once, at creation, to the protocol |\n| new ticker fee | 0.0015 ETH more | once, when a ticker is invented; it becomes the working dollars of the ticker's own pool |\n| pool fee | 1% base plus the creator's tax, 0 to 2% at launch (the owner can raise that ceiling for later launches, never above 10%; TICKR itself carries 2%) | every buy and sell, taken by the pool from the swap's input |\n| pool fee, under a fixed-inventory name | 0.82% flat, no creator tax | every buy and sell, same mechanism |\n\nThe pool fee is the pool's own LP fee, fixed when the pool is created: `(baseFeeBps + creatorTaxBps) * 100` in Uniswap's hundredths of a bip, so 10000 for 1%. It is part of the pool key and can never change. Buys pay it in the pair, sells pay it in the coin, and it accrues to the locked launch position.\n\n### The frozen fee on the market path\n\nA coin launched under a fixed-inventory name pays 82 bps, which is 8200 pips, and its creator tax is zero.\nThat is not a default. `MarketTickerLauncher` reads the launch config it was handed and reverts with\n`NotTheFrozenFee(baseFeeBps, creatorTaxBps)` unless both numbers are exactly the frozen ones.\n\nThe check exists because the factory does not do it. `launchTokenWithPair` gates only on whether the caller is a\nregistrar, and the config id is whatever the caller passed. Without the launcher's own check, an authorised\nregistrar could open a coin at any fee the factory happened to have configured. So the launcher refuses instead\nof trusting, and the fee a market-path coin carries is the same for every one of them, forever.\n\n## FeePolicy\n\n```solidity\nstruct FeePolicy {\n    address protocolFeeRecipient; // the protocol's recipient, a BuybackTreasury, frozen into every launch\n    uint16 creatorShareBps;       // of the base fee: 5000 under a redeemable name, 6000 elsewhere\n    uint16 clubShareBps;          // 1000 under a redeemable name, 0 elsewhere; the three always sum to 10000\n    uint16 protocolShareBps;      // 4000\n    uint16 buybackBurnBps;        // 0 at launch: the vault route is off. the treasury's buyback and burn of TICKR is a separate mechanism, and on\n    uint16 hookFeeBps;            // the base fee in bps, 100, copied from the config at launch\n    uint16 maxInternalPriceImpactBps; // unused in this version\n}\n```\n\nThe policy in force is copied into the launch record at creation. If the pair has no club, the club's share folds\ninto the creator's, so the split a launch shows is the split it keeps. The owner can change the policy for future\nlaunches only.\n\nA fixed-inventory name has no club. The ticker club belongs to `TickerLauncher`, and a name it did not invent is\nnot in it. So a coin launched under a fixed-inventory name freezes at **60% creator, 0% club, 40% protocol**: the\n1000 bps club share folds into the creator's 5000. Nothing was configured to make that happen; it is what\n`Factory._launch` already did for any pair without a club.\n\nTwo treasuries are now in the records, and which one a coin pays is decided once, at its launch, and never again.\n\n| launched | protocol fee recipient | what it does with a name it collects |\n| --- | --- | --- |\n| before the market release | `BuybackTreasury` `0x60C1276f...` | mints and redeems a redeemable name one for one; a name it cannot convert is forwarded to the team whole |\n| from the market release on | `BuybackTreasuryV2` `0x8dcaBBf0...` | converts a fixed-inventory name through that name's own market, so the buyback share is not lost to the team |\n\nThat was the reason for the second treasury. The first one has no way to value a name that is not a dollar, so\nevery fixed-inventory name's fees would have gone to the team in full and the burn share of them would have been\nzero. This does not mean the old treasury allocated zero overall; it allocated zero out of that particular\nrevenue.\n\n## Collecting\n\n`LaunchLocker.collectFees(token)` is permissionless. It performs a zero-liquidity decrease on the launch position and takes both currencies to the locker, then splits each side:\n\n1. The pool fee is base plus tax. The base part of what came in is `amount * baseFeeBps / (baseFeeBps + creatorTaxBps)`; the rest is the creator's tax and is the creator's alone.\n2. The base part of the quote side goes 40% to the protocol's escrow balance, 10% to the ticker club when the pair is a redeemable name, and the remainder to the creator's escrow balance. The club is paid by transferring the wrapper to `TickerLauncher` and calling `onClubFee`, and the volume that fee stands for is recorded with `recordVolume`. A club that cannot book its slice hands it to the protocol instead of blocking the collection.\n3. The coin side splits the same way. The base part goes 40% to the dead address for the protocol, 10% to the dead address for the club when there is one, and the remainder to the creator's escrow balance in the coin; the tax part goes whole to the creator's escrow balance in the coin. So every sell still takes coins out of circulation, the protocol's and the club's shares of it, and creators keep their own. Every coin launched here deflates on every sell.\n\n`FeesCollected` reports every figure of a collection. `LaunchLocker.pendingFees(token)` shows what is owed before one.\n\nThe protocol's 40% is paid to the `BuybackTreasury`, not to a wallet: half of it buys and burns TICKR, half funds the team. The half and half split between buybacks and the team applies to revenue the treasury can convert to USDG, which is USDG, ETH and redeemable names; fees paid in an asset it cannot convert at par, a Stock Token or a coin used as a quote, are forwarded to the team in full; and the protocol's and the club's sell-side shares, paid in the launched coin, are burned by the locker directly and never reach the treasury. See [13 the official coin](./13-official-coin.md). Inside a coin's first five seconds, a buy pays a snipe tax on the coin side too, burned to the dead address like every other coin-side fee; see [02 lifecycle](./02-lifecycle.md). The club a launch pays is frozen with its split: `FeePolicy.club` is written at launch and the locker pays that address and no other. A later change of the factory's club, or a club that is not a contract, touches no existing launch; a slice with nobody to book it goes to the protocol.\n\n## Who collects\n\nAnyone may call `collectFees(token)` at any time and pays only the gas, and that is the only thing that makes a collection happen.\n\ntickr ran a keeper for this, a wallet with no power over anything that called `collectFees` on a schedule. **It is switched off.** Nothing collects automatically now. Fees accrue on the locked position until somebody calls `collectFees`, and anybody may. Do not rely on a collection happening on its own, and do not assume `pendingFees` will be swept for you.\n\n## Claiming: FeeEscrow\n\nEscrow balances are pull only. `claim()` pays the caller its ETH balance, `claimToken(token)` its balance in that token. Nothing is pushed to anyone, so a recipient that rejects transfers blocks only itself.\n\n## Moving where fees go\n\nThe creator fee wallet can hand its role to another wallet with `transferCreatorFeeRecipient(token, newRecipient)`, from the wallet that holds it. Collections from then on credit the new wallet; nothing already credited in the escrow moves, so claim first. Club rewards the coin has earned but not yet claimed go to whoever holds the fee wallet when `claimClub` runs, since the club pays the recipient current at claim time. It cannot be undone by anyone but the new wallet, or by the owner's proposal path below, which a move by the creator does not cancel. The coin's own exemption from the snipe tax follows the new wallet. The site shows this as one row on the fees tab, to the creator only. The owner's slower path for lost keys and stolen wallets, a public three day notice and then a three day window in which anyone may execute, is in [09 risks](./09-risks.md); while one is pending the coin's page says so.\n\n## The ticker club\n\nUnder a redeemable name, 10% of the base fee on the quote side of every coin, what buys pay, goes into that coin's pot for the current thirty day epoch (the coin side's club share is burned, not pooled), and the creators of the other coins under the same ticker claim from it in proportion to their booked volume: when a coin's fees are collected, the locker books the quote fees collected divided by the pool fee rate as that coin's volume for the window of the collection. so it stands for buy volume in the quote, since only buys pay fees in the quote, and a coin's weight for a window depends on when someone collected. Details in [05 anchors](./05-anchors.md).\n\n## BuybackVault\n\n`buybackBurnBps` is zero in the policy at launch, so nothing is routed to the vault. That is one mechanism; the treasury's buyback and burn of TICKR, half of the protocol's share, is another and is on from the first launch, see [13 the official coin](./13-official-coin.md). The vault exists so a future policy can route a share of new launches' fees into it; the owner's only power over it is to set the strategy that may release what it holds.\n",
  "07-addresses.md": "# 07 · Addresses\n\n## Chain\n\n| | |\n|---|---|\n| Network | Robinhood Chain (mainnet) |\n| Chain id | `4663` |\n| RPC | `https://rpc.mainnet.chain.robinhood.com` |\n| Explorer | `https://robinhoodchain.blockscout.com` |\n| Gas token | ETH |\n| WETH | `0x0Bd7D308f8E1639FAb988df18A8011f41EAcAD73` |\n| USDG | `0x5fc5360D0400A0Fd4f2af552ADD042D716F1d168` (6 decimals) |\n\ntickr curves and pools use native ETH (`address(0)`), not WETH. WETH is listed for wallets and aggregators only.\n\n## Canonical Uniswap v4 on Robinhood Chain\n\nThese are the deployments tickr calls. `LaunchSeeder`, `LaunchLocker`, `CoinQuoteLauncher`, `MarketQuoteLauncher` and `TickerLauncher` are constructed with them as immutables. `ManagedTickerHook` is deployed with the stack too: every redeemable name's own pool is created behind it, and it holds nothing. A fixed-inventory name's pool has no hook.\n\n| Contract | Address |\n|---|---|\n| PoolManager | `0x8366a39CC670B4001A1121B8F6A443A643e40951` |\n| PositionManager | `0x58daec3116aae6D93017bAAea7749052E8a04fA7` |\n| Permit2 | `0x000000000022D473030F116dDEE9F6B43aC78BA3` |\n\n## Uniswap v3 (reference only)\n\nNot used by tickr. Listed because wallets and routers on the chain may quote through them.\n\n| Contract | Address |\n|---|---|\n| v3 Factory | `0x1f7d7550B1b028f7571E69A784071F0205FD2EfA` |\n| v3 NonfungiblePositionManager | `0x73991a25C818Bf1f1128dEAaB1492D45638DE0D3` |\n| v3 SwapRouter | `0xCaf681a66D020601342297493863E78C959E5cb2` |\n| v3 QuoterV2 | `0x33e885eD0Ec9bF04EcfB19341582aADCb4c8A9E7` |\n\n## tickr v1 contracts\n\nDeployed and live. Wiring is immutable, so these never change for v1; a v2 will be a new table. The same\naddresses are in `contracts/deployments/4663.json`, which is what the site is built from.\n\n| Contract | Job | Address |\n|---|---|---|\n| `Factory` | Launches, records, owner terms, the CTO timelock; `launchToken`, `getLaunchedToken`, `poolKeyOf` | `0x12EF55f994E6eb6bd55eF55Ce63800cD4425A03f` |\n| `LaunchDeployer` | CREATE2 coin; `predictToken(initiator, params, supply)` | `0xD86C1Cc523256519Dbd608318395e0C97e0368d6` |\n| `LaunchSeeder` | Opens a launch pool at the opening price with the supply in a locked one-sided position; `swapExactIn` and `swapExactInBounded` on any pool | `0x3733576410312D34B53F90cFE513B0D0995aB6Ca` |\n| `FeeEscrow` | Pull-based protocol + creator balances (native + ERC-20) | `0xCf706542a17ee6C0Cc9595E3f49d9131aF3331ba` |\n| `LaunchLocker` | Holds every launch position; `collectFees` splits the pool's fees per the frozen terms; no withdrawal | `0xDfD29cB10Ff0491CdF7896F75e54f4357F4a42b8` |\n| `LaunchAndBuyRouter` | Create + first buy in one tx (must be a factory registrar) | `0x22EC456D584cB92bdFA13469E70936e3192fd746` |\n| `AnchorRegistry` | ETH / USDG / official Stock Token allowlist | `0x7927cB22b4C5AA0DBdd851805d3149AAC07e2BC1` |\n| `BuybackVault` | Receives buyback slices; strategy interface only in v1 | `0x8c3ed4F9616893AFE2608079D2Bb02EAaCD099E5` |\n| `BuybackTreasury` | The fee recipient frozen into every launch made before the market release, TICKR's included: collects the protocol share, pays the team half, buys TICKR with the other half and burns it. No owner | `0x60C1276ff7fEB5bC15f6f05C23D9de12fa0E72BA` |\n| `BuybackTreasuryV2` | The fee recipient for launches made from the market release on. Same halves, and it converts a fixed-inventory name through its own market instead of forwarding it to the team | `0x8dcaBBf003e3d8C187D18f0a38cB1ac906D3FE28` |\n| `TickerLauncher` | Invents redeemable names (one-for-one USDG wrappers, each running its own dollar pool) and launches coins under them; a factory registrar and the ticker club; no owner | `0x7f6c8bA781b5bDC499F2BA7501A2178508877649` |\n| `ManagedTickerDeployer` | Deploys each redeemable name's wrapper at its CREATE2 address, for the ticker launcher alone; holds the wrapper's creation code so the launcher stays under the size limit | `0xe773BC7a4710DcAB084754bD296a84AF7ed9e4E7` |\n| `ManagedTickerHook` | The one hook on every redeemable name's own pool: hands each pool's callbacks to its wrapper, so the wrapper tops up its offer before a swap and checks the price and its backing after; only the wrapper touches its pool's liquidity | `0x3adE2d75475e3262A4dfd1b55c012d39d704eAC0` |\n| `MarketTickerLauncher` | Invents fixed-inventory names and launches coins under them; a factory registrar; no owner. Refuses any launch whose fee is not the frozen one | `0x901c693f5888537e3556AC0e691B0102825e2Ff9` |\n| `MarketTickerDeployer` | Holds the fixed-inventory name's creation code and the shape of its market: supply 500,000,000 at 6 decimals, pool fee 500, spacing 10, width 30. Issues only for the launcher above | `0x384fE3D736583597A1fc345758462933455A9516` |\n| `QuoteRegistry` | Tells the two kinds of name apart by which issuer made them, so nothing has to trust a name's own answer | `0x771d559712C7580d19432f83A3A7Eb7C6dFFbdCF` |\n| `CoinQuoteLauncher` | Mode 3: launches priced in a coin launched here, opening cap from its pool | `0x48535E372642810a69DC7e11A2a5C2709B8119D0` |\n| `StockQuoteLauncher` | Mode 4: launches priced in a Stock Token, opening cap from its Chainlink feed | `0x719EDF3aE0aB42B9657983c617aA1b16aCDEd4b0` |\n| `MarketQuoteLauncher` | Mode 5: launches priced in any token with a deep enough Uniswap v3 pool against WETH or USDG. Deployed at `0x4Dc4404ac80fC4372B689f0013b8adACf1E0B61b` but not wired into the site, so the mode is not offered | not in use |\n| `ZapRouter` | Buy or sell any coin with ETH in one transaction, whatever it is quoted in | `0xeB3E56b10C58e1149Ae38B65E0B28A90cC33ffe4` |\n| Universal Router | The two activation buys a fixed-inventory launch ends with | `0x8876789976dEcBfCbBbe364623C63652db8C0904` |\n| v4 Quoter | Quotes used by the site; reads only | `0x8Dc178eFB8111BB0973Dd9d722ebeFF267c98F94` |\n\nLogs start at block `57108282`.\n\n## The official coin\n\n| | |\n|---|---|\n| TICKR | `0x51d553Efd2E8D772AEe6d602A9ea26C56c9a6942` |\n| FUN, the name it is priced in | `0xF9d30A05A63d795e3eF37b34143f33b2cBEf0f14` |\n\nFUN is a redeemable name, not a fixed-inventory one: it predates the market release and `QuoteRegistry.kindOf`\ndoes not classify it. It stays one for one with USDG for as long as it exists. See\n[13 the official coin](./13-official-coin.md).\n\n\nA coin's `Token` contract is created at launch time, and a name the first time its symbol is used: a\n`TickerToken` from `TickerLauncher` if it is redeemable, a fixed-inventory name from `MarketTickerLauncher` if\nit is not. Find them from `TokenLaunched`, `TickerCreated` and `NameCreated` events, or from\n`Factory.getLaunchedToken(token)`, `TickerLauncher.isTicker(addr)` and `QuoteRegistry.kindOf(addr)`.\n\n`BuybackTreasury` is deployed before the factory, because the factory names it as the protocol fee recipient and that address is frozen into every launch from the first one on. It has no owner, so it is not in the handover below. The deploy runs in a fixed order: `Deploy.s.sol`, then `RegisterStockTokens.s.sol`, then `Genesis.s.sol`, all signed by the deployer while it is still the owner; only then does the owner wallet call `acceptOwnership()` on the six owned contracts (`Factory`, `AnchorRegistry`, `BuybackVault`, `CoinQuoteLauncher`, `StockQuoteLauncher`, `MarketQuoteLauncher`). Genesis opens launches and removes the deployer's own pass. Start `getLogs` from `startBlock`. The owner is the deployer key unless `OWNER` was set, in which case ownership of `Factory`, `AnchorRegistry`, `BuybackVault`, `CoinQuoteLauncher`, `StockQuoteLauncher` and `MarketQuoteLauncher` is offered to it (`TickerLauncher` has no owner) and must be accepted with `acceptOwnership()` on each.\n",
  "09-risks.md": "# 09 · Risks\n\nRead this before launching, buying, or integrating. Nothing here is advice.\n\n## Market and token risks\n\n- **Names and symbols are not unique.** Anyone can launch \"PEPE\" with the same logo. The only identity is the token address. Verify it against `Factory.getLaunchedToken(token).exists` and the `TokenLaunched` log from the tickr factory address before trading.\n- **Creator tax.** A launch may carry a `creatorTaxBps` of up to `maxCreatorTaxBps()` (2% at launch; the owner can raise that ceiling for later launches, never above 10%; TICKR itself carries 2%) on every trade in its pool and every pool swap, forever. Read `getLaunchedToken(token).creatorTaxBps` before buying. It cannot be changed after creation, in either direction.\n\n## Pair risks\n\n- A launch quoted in an ERC-20 inherits that asset's contract, issuer, transfer-restriction, and depeg risk in full. The protocol cannot change a launch's pair. See [04 Official pairs](./04-custom-pairs.md).\n\n## Redeemable name risks\n\n- The way back to USDG from a coin under a redeemable name is the coin's own pool, then a one-for-one redeem of the name (`TickerToken.redeem`). The redeem has no price impact and no depth limit: it returns exactly what the name wraps. The only price impact on the way out is the coin's own pool, the same as for a coin paired with USDG directly.\n- The risk that remains is the coin's own pool. A coin under a redeemable name can go to zero like any other coin; the name it is priced in stays worth exactly the USDG it wraps.\n- Nobody can sell supply they were handed at birth, because nobody was handed any.\n\n## Fixed-inventory name risks\n\nThese are different risks, not smaller ones. Read them before launching under one.\n\n- **The quote asset can fall.** A fixed-inventory name holds nothing and is worth what its market says. A coin can hold its price in the name and still lose most of its dollar value, because the name lost it. A redeemable name cannot do this to you. This is the trade you are making.\n- **There is no redeem.** The only way out of the name is selling it in its own pool, which has price impact and finite depth. A large exit moves the price against itself. `TickerToken.redeem` does not exist here and there is no equivalent.\n- **Both markets can be thin.** Getting from a coin to dollars crosses two pools, the coin's and the name's. Depth in one does not imply depth in the other, and the site quotes the whole route rather than either half.\n- **A launch can be refused for lack of a route.** If the name's market has nothing available in the direction a trade needs, the router stops rather than filling badly. On the site that reads: *This route currently has no available liquidity in the required direction.* It is a refusal, not a failure, and a different size or a later attempt needs its own fresh quote.\n- The disclosure, on the docs index and the terms page: *Not issued by Robinhood Assets. Not a Stock Token. No mint/redeem against listed shares. A name invented on tickr is not the asset it is named after: a redeemable name is worth exactly the USDG it wraps, a fixed-inventory name has no backing and is worth only what its own market says.*\n\n## The buyback cannot be switched off\n\nStated plainly because it is permanent and it surprises people.\n\n- `buy()` on either treasury has **no permission check**. Anybody may call it and burn a tranche. The keeper the team runs is a convenience, not a gate: stopping the keeper stops the team from calling it, and stops nobody else.\n- `collect()` has no permission check either, so anybody can pull accrued fees into the treasury and refill the earmark.\n- The burn share **only rises**. `proposeBuybackShare` reverts unless the new value is higher than the current one, and no function lowers it. It starts at 50% and 50% is therefore its floor for the life of the contract.\n- There is **no pause, no sweep and no withdrawal**. Dollars earmarked for the burn cannot be recovered by anyone, including the owner.\n\nThe consequence: once a coin's policy is frozen to a treasury, the protocol's share of that coin's fees will fund\nthat treasury's burn for as long as the coin trades, and no transaction exists that changes it. The owner can\npoint *future* launches at a different recipient. It can do nothing about coins already launched.\n\n## Third party liquidity and other pools\n\nA tickr pool is a plain Uniswap v4 pool. Anyone may add liquidity to it and remove their own again, and anyone may open another pool for the same two tokens at another fee or price. Neither touches the locked launch position, and a pool nobody trades in prices nothing. Snipe protection is a tax, not a gate: a buy out of the pool in the coin's first five seconds pays 99% in the launch second, 25% one second in, 3% at two, then dust, then nothing, all of it burned to the dead address. The launcher and its fee wallet are exempt, sells are never taxed, and from the fifth second the tax is gone. Launch protection sits beside it, counted in blocks: nobody but the launch's own wallets may buy in the launch block, and for the next two blocks every other wallet is held to 5% of supply held and 5.5% bought. A transfer into a wallet inside those two blocks is held to the same 5%. From the fourth block the coin is a plain ERC-20. Bots can still buy from the second block on, at that price and under those caps, and both rules only see coins that leave the pool manager as ERC-20 transfers: a bot that settles its swap into ERC-6909 claim balances inside the pool manager and withdraws after the window pays neither the tax nor the caps. The launcher's own wallets are exempt and may pass coins on, five percent per wallet, in the launch block. Take the rules as a cost on the usual routes, not as a promise that nobody snipes.\n\n## Liquidity is locked forever\n\nThe launch position of every coin is minted straight to `LaunchLocker` and never leaves it. The contract can\nreceive a position and collect its fees, and it has no function that moves, reduces or rescues principal. Not for\nthe creator, not for the protocol, not for the factory's owner, who is not on the locker at all.\n\nThis cuts both ways, and that is the risk on this page rather than the reassurance: **liquidity that cannot be\npulled cannot be recovered either.** There is no migration, no redeploy onto a fixed pool, and no recourse if a\nlaunch was opened on terms the creator later regrets. A fixed-inventory name's own issuance is held harder still,\nby a per-name locker with no fee collection at all. Full detail in [14 liquidity lock](./14-liquidity-lock.md).\n\n\n\n## Owner powers, exactly\n\nThe factory is `Ownable2Step`. The owner can change the terms of future launches and nothing about a coin that exists. Function by function:\n\n| power | function | reach |\n| --- | --- | --- |\n| Launch fee | `setLaunchFee` | Future launches only. |\n| Creator tax ceiling | `setMaxCreatorTaxBps` | Future launches only. 2% at launch; the owner can raise it, never above 10%. A launch keeps the tax it was created with. |\n| Launch configs | `addLaunchConfig`, `setLaunchConfig` | Supply, base fee, opening quote reserve and tick spacing of future launches. |\n| Pair economics | `setPairTokenEconomics` | Which ERC-20s can be a pair through the factory and their opening quote reserve; future launches only. Revoking USDG stops USDG-quoted launches through the factory and, since redeemable names read it, new ticker launches too. |\n| Open or close launching | `setLaunchEnabled`, `setWhitelistedLauncher` | New launches only. Every existing pool keeps trading. A factory is born closed; genesis opens it and removes the deployer's own pass, so closing again later closes it for everyone. |\n| Registrars | `setRegistrar` | Which contracts may launch with their own pair economics: the ticker, Stock Token, coin and market quote launchers. |\n| Fee policy | `setFeePolicy` | The split of future launches. Every existing launch keeps the policy copied at its creation, including the protocol wallet. |\n| Fee club | `setFeeClub` | Which contract books the club's share for future launches; the club is frozen into each launch's policy at launch. |\n| Ticker launcher | `setTickerLauncher` | Which contract the treasury and the zap treat as the register of names. The names' pools answer to the launcher they were created by, whatever this points at. Set once at deploy; independent of the club. Existing launches keep the club they launched with, and a club that fails to book a fee sends that slice to the protocol rather than blocking a collection. |\n| Move a fee wallet | `proposeCreatorFeeRecipient`, `executeCreatorFeeRecipientChange` | Any coin's creator fee wallet, after a public three day notice and within a three day window. Exists for lost keys and compromised wallets; every use is an event. |\n| Raise the burn share | `BuybackTreasury.proposeBuybackShare`, `applyBuybackShare` | the burn share starts at 50%, can be raised by the owner after a three-day delay, and can never be lowered. The proposal is the treasury's only gated call; applying it is anyone's. |\n| Set a name's conversion floor | `BuybackTreasuryV2.setMinRate(token, toCounter, rateX96)` | The rate a conversion of that name must beat before the treasury will do it. **This has no upper or lower bound.** The owner may set a floor below the frozen policy, weakening the protection for that one name, or high enough that no conversion of it can ever succeed, which stops the treasury converting it at all. Stated here rather than implied away. |\n| Set the default conversion floor | `BuybackTreasuryV2.setDefaultMinRate(toCounter, rateX96)` | The floor for names with none of their own. May be raised freely and lowered again, but never below the frozen policy value. Unlike `setMinRate` it cannot go under that. |\n| Reserve a ticker or a name | `AnchorRegistry.reserveTicker`, `reserveNames` | Future launches only: a reserved symbol cannot be invented as a ticker and a reserved name cannot be used by a new coin. |\n| Stock Token, coin and market targets | `StockQuoteLauncher.setTargetRaiseUsd`, `setMaxStaleness`, `CoinQuoteLauncher.setTargetRaise`, `MarketQuoteLauncher.setTargetRaise`, `setMinDepth` | The opening market cap of future launches in those modes. |\n| Give up ownership | `renounceOwnership` | Freezes the terms of future launches forever. Cannot be undone. |\n\nWhat the owner cannot do: take anything out of a pool, a locked position, the fee escrow, or a ticker's USDG reserve; stop an existing coin from trading; change an existing launch's fee, tax, pair or economics; mint, freeze or blacklist a coin; block a fee collection. There is no rescue function and no phase in which funds wait on anyone: the pool exists in the launch transaction and the position is locked in it.\n\nRelated owners outside the factory: the `AnchorRegistry` owner can `register` / `setActive` anchors (new launches only); the `BuybackVault` owner can `setStrategy`, and only that strategy can release what the vault holds, which is nothing while `buybackBurnBps` is zero. The `BuybackTreasury`, which receives the protocol's fee share, has no owner at all: no admin, no withdrawal, no rescue. Its money leaves only to the team wallet and the dead address, by the rules in [13](./13-official-coin.md).\n\n## Immutable\n\nThe contracts are immutable; a bug cannot be patched in place, only mitigated by deploying a new version and stopping new launches on the old factory (`setLaunchEnabled(false)`), which does not affect existing curves or pools. Interact only with amounts you can afford to lose.\n",
  "10-coin-quotes.md": "# 10 · tickr coins (Mode 3)\n\nA launch can be priced in **another coin this factory already launched**, instead of ETH, USDG, a Stock Token, or a\nDIY quote. Contract: `CoinQuoteLauncher`.\n\n## Why\n\nBuying the new coin requires holding the quote coin, so every buy is demand for it. The new\ncoin seeds a `NEW/QUOTE` Uniswap v4 pool whose position goes to `LaunchLocker` and can never be withdrawn, so the quote\ncoin's market gets permanently deeper. A community can let people launch coins on top of their coin.\n\n## Guardrails\n\nChecked in this order by `quotePrice`, which every other entry point routes through:\n\n| # | Rule | Error | Why |\n|---|------|-------|-----|\n| 1 | The quote must be a coin this factory launched | `QuoteNotLaunchedHere` | Third-party tokens can charge fees on transfer, rebase, or blocklist. `Curve` assumes it receives exactly what was sent, so any of those silently corrupt `realQuote` and strand the launch. Our own `Token` has no transfer hooks and a fixed supply. |\n| 2 | The quote coin must itself be priced against an approved anchor | `QuoteTooDeep` | Depth one only. Without this a launch could sit on a tower of launches, where the bottom falling to zero destroys everything above it. A DIY quote is never an anchor, so a DIY launch can never be used as a quote. |\n| 3 | The quote coin's pool must have liquidity at the current price | `QuotePriceUnavailable` | Otherwise it has no readable price. |\n| 4 | The threshold is sized from the quote's live pool reserves and pinned | `LaunchEconomicsMismatch` | `previewLaunch` returns the hash to pass as `expectedEconomics`, so terms cannot move between the quote a creator reads and the transaction they send. |\n\n`isEligibleQuote(coin)` returns a bool and never reverts, so a front end can filter the whole launch list on-chain.\n\n## Economics\n\n`quoteEconomics` reads the quote coin's pool (`slot0` and the liquidity in range at the current price) and derives\nits reserves, `rCoin` of the coin and `rBase` of the base asset. The threshold is the amount of the quote coin that\nthe base asset's target raise buys out of that pool at constant product:\n\n```\nthreshold = rCoin * target / (rBase + target)   // quote-coin units, 18 decimals; target = targetRaise[baseAsset]\nphantom   = threshold * 40%\n```\n\nThe model holds that liquidity constant through the trade. The launch position, from the opening price to the end\nof the range, is exactly that; positions other people add or remove change what an actual buy of the target would\ncost, in either direction and by however much they add or remove. The threshold is a sizing convention for the\nopening market cap of the new coin, pinned at launch; it is not a quote anyone trades at and it promises nothing\nabout the executable value of the quote coin. The result is pinned in the preview, so a creator sees the price it gives before\nsigning, and a launch whose quote pool has moved since the preview reverts.\n\nSpot alone, `target / price`, would ask for the whole reserve or more whenever the base reserve is near the target, which no buyer could ever fill. Sizing the threshold as a buy out of the reserve keeps it to what the quote's market can actually deliver, however deep or shallow that market is, and each later coin under the same quote sees a threshold sized to what is left.\n`previewLaunch` also returns the spot price from `quotePrice` (`priceX18`, base-asset units per `1e18` of the coin,\ninverted when the coin is `currency1`) for display.\n\n`targetRaise` is set per base asset by the owner, so a launch priced in any coin costs a comparable amount of real money to move, whichever coin it is priced in.\n\n## Calling it\n\n```solidity\n(bytes32 expectedEconomics, PairEconomics memory econ, address baseAsset, uint256 priceX18)\n    = coinQuoteLauncher.previewLaunch(launchConfigId, quoteCoin);\n\n// params.expectedEconomics = expectedEconomics, so the launch reverts if the quote pool moved since the preview\n(address token, bytes32 poolId) = coinQuoteLauncher.launchWithCoinQuote{value: factory.launchFee()}(params, launchConfigId, quoteCoin);\n\n// or with the creator's first buy in the same transaction: `coinIn` of the quote coin, approved to the launcher first\n(address token2, bytes32 poolId2, uint256 tokensOut) =\n    coinQuoteLauncher.launchWithCoinQuoteAndBuy{value: factory.launchFee()}(params, launchConfigId, quoteCoin, coinIn, minTokensOut);\n```\n\nEvent: `CoinQuoteLaunched(token, poolId, quoteCoin, baseAsset, quotePriceX18, phantomQuote)`.\n\nFor a first buy in the same transaction, use `launchWithCoinQuoteAndBuy`, described below.\n\n## What it inherits\n\nEverything the factory gives every launch: the same pool, the same locked position, the same fee split and the same snipe tax and launch protection, and the creator is paid in the coin their launch trades against, like every other mode. `CoinQuoteLauncher` holds no\nfunds and has no privilege beyond being a registrar on the factory.\n\n## Risks\n\nThe new coin carries the quote coin's risk on top of its own. If the quote coin's market collapses, the new coin's\nliquidity collapses with it, and the pool cannot be migrated because it is locked. Prices are denominated in a volatile\nasset, so a chart can rise against the quote coin while falling in dollar terms.\n\n## The creator's first buy, the dev buy\n\n`launchWithCoinQuoteAndBuy` launches and buys in one transaction. Approve the launcher for `coinIn` of the quote\ncoin first, and send `factory.launchFee()` as value; anything else reverts `BadValue`.\n\nThe launcher pulls `coinIn` from you, launches, then swaps it in the new pool through `LaunchSeeder.swapExactIn`\nwith the coin delivered **to you**, not to itself. So the snipe tax and the launch-block caps see your wallet, the\nsame as any other buyer, and `minTokensOut` is yours to set: pass a real one, since a first buy on a brand new\npool has no reference price to fall back on. What the pool did not take is returned to you, measured against the\nlauncher's balance before your funds arrived, so nothing it held beforehand can leave with the refund.\n\nIt emits `FirstBuy(token, quoteIn, tokensOut)` alongside the launch event. The launcher holds no funds between\ntransactions and has no privilege beyond being a registrar on the factory.\n\n\n",
  "11-stock-quotes.md": "# 11 · Stock Token quotes (Mode 4)\n\nA launch can be priced in an official Stock Token, with the opening market cap sized from that asset's live\nChainlink feed. Contract: `StockQuoteLauncher`.\n\n## Why a feed and not a fixed number\n\nThe threshold is denominated in the quote asset. A fixed number of tokens would mean incomparable raises: ten tokens\nof an $11 stock and ten of a $1,600 stock are not the same ask. So the owner sets **one USD target**, and every launch\nconverts it through its own asset's feed.\n\n```\nthreshold = targetRaiseUsd * 10^tokenDecimals / price\nphantom   = threshold * 40%\n```\n\n`targetRaiseUsd` and `price` share the feed's decimals, which is 8 on this chain, so those cancel and the result is in\ntoken units. At an $8,090 target: NVDA at $224.01 needs 36.11 tokens, AAPL at $326.32 needs 24.79. Both are worth the\nsame.\n\nThe feed answer is the price of **one token**, with the corporate-action multiplier already applied. Do not apply\n`uiMultiplier()` on top of it.\n\n## Guardrails\n\nChecked in order by `stockPrice`, which every other entry point routes through:\n\n| Rule | Error |\n|------|-------|\n| The asset is an active, official Stock Token in `AnchorRegistry` (kind 2) | `NotOfficialStockToken` |\n| It has a feed registered against it | `NoFeed` |\n| The feed answer is positive | `BadPrice` |\n| The feed is fresher than `maxStaleness`, three days at deploy so Friday's close still counts through the weekend, and its round is complete (`answeredInRound >= roundId`) | `StalePrice` |\n| Terms are pinned through `previewLaunch` before launching | `LaunchEconomicsMismatch` |\n\n`isEligibleQuote(token)` returns a bool and never reverts, so a front end can filter the list on-chain.\n\nA same-ticker impostor cannot get in: eligibility is decided by the registry, not by the token's own metadata.\n\n## Registering the assets\n\nThe registry is owner-curated. `config/stock-tokens-4663.json` holds the verified set, and\n`script/RegisterStockTokens.s.sol` registers them in one batch through `AnchorRegistry.registerMany`.\n\nNothing is registered on the strength of the file alone. The script re-checks every entry against the chain first:\nthe token's `symbol()` must equal the ticker, `decimals()` must be 18, and the feed must return a positive answer.\nEntries that fail are skipped and logged.\n\nThe list itself was built by cross-referencing the Chainlink feeds published for this chain against the tokens\ndeployed on it, then verifying each candidate on-chain. The marker that separates a Stock Token from a crypto asset is\nERC-8056: Stock Tokens implement `uiMultiplier()`, crypto assets do not. That yielded 34 assets.\n\n## Calling it\n\n```solidity\n(bytes32 expectedEconomics, PairEconomics memory econ, uint256 priceUsd)\n    = stockQuoteLauncher.previewLaunch(launchConfigId, stockToken);\n\n// params.expectedEconomics = expectedEconomics, so the launch reverts if the feed moved since the preview\n(address token, bytes32 poolId) =\n    stockQuoteLauncher.launchWithStockQuote{value: factory.launchFee()}(params, launchConfigId, stockToken);\n\n// or with the creator's first buy in the same transaction: `stockIn` of the Stock Token, approved to the launcher first\n(address token2, bytes32 poolId2, uint256 tokensOut) = stockQuoteLauncher.launchWithStockQuoteAndBuy{\n    value: factory.launchFee()\n}(params, launchConfigId, stockToken, stockIn, minTokensOut);\n```\n\nEvent: `StockQuoteLaunched(token, poolId, stockToken, priceUsd, phantomQuote)`. For a first buy in the same\ntransaction, use `launchWithStockQuoteAndBuy`, described below.\n\n## What it inherits\n\nEverything the factory gives every launch: the same pool, the same locked position, the same fee split, and the\nsame snipe tax and launch protection. The creator is paid in the Stock Token their launch trades against, and its\npool is paired against it. `StockQuoteLauncher` holds no funds and has no privilege beyond being a registrar on\nthe factory.\n\n## Risks\n\nThe launch carries the underlying equity's risk on top of its own, including market hours and corporate actions. If a\nfeed goes stale, new launches against that asset are refused, but launches already trading are unaffected: the\nthreshold was fixed at creation. The owner can deactivate an asset, which stops new launches only.\n\n## The creator's first buy, the dev buy\n\n`launchWithStockQuoteAndBuy` launches and buys in one transaction. Approve the launcher for `coinIn` of the Stock Token first, and send `factory.launchFee()` as value; anything else reverts `BadValue`.\n\nThe launcher pulls `coinIn` from you, launches, then swaps it in the new pool through `LaunchSeeder.swapExactIn`\nwith the coin delivered **to you**, not to itself. So the snipe tax and the launch-block caps see your wallet, the\nsame as any other buyer, and `minTokensOut` is yours to set: pass a real one, since a first buy on a brand new\npool has no reference price to fall back on. What the pool did not take is returned to you, measured against the\nlauncher's balance before your funds arrived, so nothing it held beforehand can leave with the refund.\n\nIt emits `FirstBuy(token, quoteIn, tokensOut)` alongside the launch event. The launcher holds no funds between\ntransactions and has no privilege beyond being a registrar on the factory.\n\n\n",
  "12-zap.md": "# 12 · Buying with ETH (the zap)\n\nEvery coin can be bought with ETH in one transaction, whatever it is quoted in. Contract: `ZapRouter`.\n\n## Why it exists\n\nThe coin's own pool, as the factory records it, must be the last hop of a buy and the first hop of a sell; a route through any other pool that happens to output the coin is refused. A coin quoted in USDG, a Stock Token, another coin, or a redeemable name is priced in that asset, and its pool only\naccepts that asset. Without help a buyer would have to acquire the quote somewhere else first. That is the friction\naggregators remove on other launchpads, and the zap removes it here.\n\n## How it works\n\nEvery route ends at the coin itself: its own pool is the last hop on a buy and the first hop on a sell. A coin priced in ETH is one hop. A coin priced in USDG is the ETH/USDG pool then the coin's pool. A coin under a redeemable name is ETH/USDG, then a wrap hop that mints the ticker one for one, then the coin's pool. A coin priced in a Stock Token goes through that stock's market first.\n\n\nThe caller passes a route: a list of hops walked from the input asset to the coin's quote. A hop is a Uniswap\nv4 pool key, a Uniswap v3 pool address, or a redeemable name to wrap into or redeem from, one for one. Consecutive v4 hops run inside a single `PoolManager.unlock`; a v3 hop calls\nthe pool directly and pays it in the swap callback. Native ETH is wrapped before a v3 hop and unwrapped before a v4\nhop that prices native ETH, so one route can mix both. A run of v4 hops is one unlock: the router swaps hop by hop, settles the run's input once and takes its output once, so nothing between the two ever leaves the pool manager. The coin's own pool, the last hop, pays the recipient directly; the snipe tax and the launch caps therefore see the buyer, never the router, and the amount the zap reports is the recipient's balance change. On a sell the coin goes from the seller straight into the pool manager. It holds nothing between transactions and has no\nprivileges.\n\n```solidity\nstruct Hop {\n    uint8 kind;           // 0 = Uniswap v4 (key), 1 = Uniswap v3 (pool), 2 = wrap (redeemable name, one for one; pool = the ticker)\n    PoolKey key;\n    address pool;\n}\n\nstruct ZapParams {\n    address token;        // the coin to buy\n    address tokenIn;      // address(0) = native ETH, amount taken from msg.value\n    uint256 amountIn;     // ERC-20 input only\n    Hop[] path;           // hops from tokenIn to the coin's quote; empty when tokenIn already is the quote\n    uint256 minTokensOut; // an absolute minimum of the coin, for the whole route\n    address recipient;\n    uint256 deadline;\n}\n\nfunction zapBuy(ZapParams calldata p) external payable returns (uint256 tokensOut);\nfunction previewZap(ZapParams calldata p) external payable; // always reverts with Preview(quoteOut, tokensOut)\n```\n\n`previewZap` runs the real path and reverts with the amounts, so a front end quotes it with a plain `eth_call`.\nThe web app does exactly that and sizes `minTokensOut` from the result.\n\n## Selling for ETH\n\nThe same router sells: `zapSell` pulls the coin, sells it on the coin's pool, walks the route backwards from the quote to\n`tokenOut`, checks `minOut`, and pays the seller. `previewZapSell` reverts with `Preview(quoteOut, amountOut)` the\nsame way. The web app simulates it with the seller's allowance overridden, so the quote is exact before anything\nis approved.\n\n```solidity\nstruct ZapSellParams {\n    address token;     // the coin to sell\n    uint256 amountIn;  // coins\n    Hop[] path;        // hops from the coin's quote to tokenOut; empty when tokenOut already is the quote\n    address tokenOut;  // address(0) = native ETH\n    uint256 minOut;    // in tokenOut units\n    address recipient;\n    uint256 deadline;\n}\n```\n\n## Routes the web app builds\n\n| Quote asset | Path |\n|---|---|\n| ETH | none, the coin's pool is called directly |\n| USDG | ETH/USDG (fee 0.01%) |\n| a redeemable name (paired with USDG) | ETH/USDG, then the name's own pool (a v4 hop, fee 500, spacing 1, hooks = `ManagedTickerHook`) or wrap into the name one for one, whichever pays more; the site quotes both. A buy above the pool's offer wraps |\n| a fixed-inventory name (paired with USDG) | ETH/USDG, then the name's own pool (a v4 hop, fee 500, spacing 10, no hook), then the coin's own. There is no wrap hop and no redeem: the name is bought and sold in its market like any other token, so both halves of the route have price impact |\n| a coin launched here | that coin's pool, no hook |\n| a Stock Token | the deepest pool against ETH: native ETH on v4 or WETH on v3, across the standard fee tiers |\n\nWhere no route is known the trade panel falls back to paying in the quote asset directly.\n\n## Guardrails\n\n| Rule | Error |\n|---|---|\n| Deadline not passed | `Expired` |\n| The coin was launched by this factory | `UnknownToken` |\n| The path ends at the coin's quote, and each hop contains the current asset | `BadPath` |\n| The v3 swap callback comes from the pool being swapped, and never asks for more than the hop holds | `OnlyPool`, `InsufficientLiquidity` |\n| Every hop fills completely | `InsufficientLiquidity` (a partial hop would strand funds in the router) |\n\n## The listing buys\n\nThe two buys that end a launch under a name ([05](./05-anchors.md)) do not go through tickr's zap: they are sent through Uniswap's canonical Universal Router (`universalRouter` in the deployment record) with an explicit path of the launch's own pool keys, as the reference did. The calldata is `execute(0x1004, [v4 swap (actions 0x070c0f: exact input, settle all, take all), sweep], deadline)` with native ETH in, the last pool's currency as the output and the wallet as the recipient; `contracts/script/lib/UniversalRouterBuy.sol` builds it for the scripts and the fork test, `web/src/lib/activation.ts` for the site, byte for byte the same. The minimum is one percent under a fresh quote from Uniswap's v4 quoter (`v4Quoter` in the record) and is never zero. A name's pool takes one visit per transaction: a route through the same name's pool twice reverts.\n",
  "13-official-coin.md": "# 13 · The official coin: TICKR, priced in FUN\n\ntickr has one coin of its own. Its ticker is **TICKR**, it is priced in **FUN**, and it is the first launch on the platform. It runs on exactly the rules every other launch runs on. This page lists what makes it different, which is only who launched it and when.\n\n## What it is\n\n| | |\n| --- | --- |\n| Coin | TICKR, name `tickr`, 18 decimals |\n| Pair | FUN, a redeemable name: a one-for-one wrapper of USDG. FUN predates the market release, so it stays this kind for as long as it exists; `QuoteRegistry.kindOf(FUN)` returns 0, unclassified, and everything that reads it treats an unclassified name as redeemable, which is what FUN is |\n| Its treasury | `BuybackTreasury` `0x60C1276f...`, the first one. TICKR's policy was frozen to it at launch and cannot be repointed. `BuybackTreasuryV2` is for launches made from the market release on and has no bearing on TICKR |\n| Supply | 1,000,000,000, all of it in the locked pool at launch; nobody holds any at creation, and the only way to hold some in the launch transaction is a first buy from the curve like anyone else |\n| Fee split | 50% creator, 10% ticker club, 40% protocol, frozen at launch, the same as every coin under a ticker |\n| Creator | the team. The creator share of TICKR's fees goes to the team's fee wallet |\n| Creator tax | 2%, the launch-time ceiling, on every trade, all of it to the team's fee wallet, shown on every trade like any creator tax |\n| Allocation, airdrop, vesting | none |\n\n## How it was launched\n\nIn the genesis transaction, while launching was still closed to everyone but the deployer: `TickerLauncher.launchAndBuy(\"FUN\", ...)` invented FUN, paid the ticker fee that became the working dollars of FUN's own pool, opened TICKR's pool with the whole supply locked in it, on the same terms as every coin after it, and made the disclosed first buy, all in that one transaction, so nothing could trade the pool before it. Two small buys followed, each its own transaction through Uniswap's canonical Universal Router, both to the deployer's wallet: 0.0005 ETH of FUN through its own pool, then 0.001 ETH of TICKR; these are the listing buys every launch under a name ends with, see below. Then launches opened to everyone.\n\n## The first buy\n\nIn the genesis transaction itself, the team bought a slice of TICKR: `5%` of supply, delivered to the treasury wallet. It is paid in FUN minted from USDG that the deployer bought with ETH the moment before, bought from the pool at the pool's price with no exemption from the fee, and the transaction is public. The size is worked out by running that same transaction in a simulation first and searching for the dollars that buy the share; the pool is new both times, so the answer is exact.\n\nOn launch night that 5% went into the pool instead of the fire: all 50,000,000 TICKR sit as a concentrated position on the TICKR/FUN pool, the same pool as every other trade, from just above the price at placement up to about fifty percent above it, so buys land on them at a fraction of the price impact the launch curve alone would give. The position is an NFT (id 2135484) owned by the treasury wallet; it earns the pool fee like any position and can be withdrawn or moved by that wallet. Nothing of it was burned, and none of it was sold. A first placement (id 2135009) was made twenty percent wide and the price fell out of its band within the hour, so it was withdrawn and placed again wider; every step below is a treasury or deployer transaction, none a sale:\n\n- the 50,000,000 TICKR moved to the deployer, then placed: `0xe3d40210b9a7f3496aa6a4607cb7d448d518b4195d5a12021b6763e9c3d3d84a`, `0xc2d6954f0d1359cf592199d27004ff6e4724656877d634f3ef7e2fb2658001b6`\n- withdrawn and placed again, wider: `0xf84487294c842c9021b93f96ce5522f31c89dd8100c94573171e9ca796a450c8`, `0x54dc325bf0cc8bcb5d5771ecdca6d6fa4bc6aa7404d2b0c3effb1d2d90dbc47f`, `0x16f5713ff400d636790fa8c36754722d1aef952da571634a4c84d03c6f7f51de`\n\n## Buybacks\n\nThe protocol's share of every fee, and every launch fee, is paid to a contract, `BuybackTreasury`, not to a wallet. It is the protocol fee recipient from the first launch, frozen into TICKR and every coin after it. The treasury has no owner and no way to withdraw: money leaves it two ways only, to the team wallet and to the dead address. The half and half split between buybacks and the team applies to revenue the treasury can convert to USDG, which is USDG, ETH and redeemable names; fees paid in an asset it cannot convert at par, a Stock Token or a coin used as a quote, are forwarded to the team in full; and the protocol's and the club's sell-side shares, paid in the launched coin, are burned by the locker directly and never reach the treasury.\n\nThe treasury binds itself to the ticker launcher the first time `collect` or `buy` runs after deployment (`launcher()`, event `LauncherBound`); from then on what FUN is, which coin is official and which wrappers convert cannot be moved by the factory owner. Anyone may call `collect(tokens)`. It claims what the escrow holds for the treasury, turns what it can into dollars (a redeemable name unwraps at par, ETH goes through the live ETH/USDG pool), sends half of the dollars to the team wallet, and sets the other half aside. Anything it cannot convert, a Stock Token or a coin used as a quote, goes whole to the team wallet, since the treasury has no honest price for it.\n\nAnyone may then call `buy()`, at most once every ten minutes. It spends at most 5% of the dollars set aside, sized so that the buy moves TICKR's pool by no more than 300 basis points at the liquidity in range when it is sized, and the swap itself carries that price as its limit: the pool stops there whatever its liquidity turns out to be, and the FUN it did not take comes back as dollars and stays earmarked. It turns those dollars into FUN, buys TICKR in the TICKR/FUN pool, and sends the TICKR to `0x000000000000000000000000000000000000dEaD`. The size, the limit and the least it will accept are computed on chain from the pool as it stands when `buy` is called: a caller chooses none of them, and what one call can do is bounded by the tranche, the interval and the price limit. The treasury has no oracle. TICKR trades in one pool, so the pool's price is the only price there is, and a buy starts from wherever that price is; someone who moves it before a buy pays the pool's fee both ways to move it and can catch at most one tranche, at most once every ten minutes, inside the bound. The ETH conversion in `collect` runs on the same interval through the deepest pool on the chain. Every buy is a `BoughtAndBurned` event, and the TICKR page shows the running total.\n\nthe burn share starts at 50%, can be raised by the owner after a three-day delay, and can never be lowered. `buybackShareBps` is the share in force; `proposeBuybackShare(bps)` is the factory owner's proposal, upward only and at most all of it, and `applyBuybackShare()` puts it in force once `SHARE_DELAY` has passed, by anyone. A newer proposal replaces a pending one and the delay starts again. Burning takes coins out of circulation. It does not guarantee a higher price.\n\n## The FUN club\n\nFUN is a redeemable name, so it has a ticker club like any other, and TICKR is in it.\n\n10% of the base fee of every coin launched under FUN is booked to that club when the fees are collected, in\n30-day epochs (`TickerLauncher.EPOCH`). Members claim their share of each pot by booked volume, and the club's\ncaptain counts double. TICKR is the founder's coin, the first launched under FUN, so it is captain in every epoch\nit has volume in; in an epoch where it has none, the coin with the most volume under FUN takes the seat instead,\nand TICKR gets it back in any later epoch it trades in.\n\nThe captain seat is a weight in a division and nothing else. It carries no say over any other coin, no access to\nany fee but its own share, and no control over the name. Nobody owns FUN. See\n[05 anchors](./05-anchors.md) for how the club, the captain and the sweep work in full.\n\nOne practical note: a pot is booked to the epoch its collection happens in, not the epoch the trades happened in,\nand nothing collects automatically any more. A coin that wants its fees booked where they were earned has to call\n`collectFees` before the epoch closes.\n\n\n\n## Where to find it\n\nThe deployment record (`contracts/deployments/<chainId>.json`) carries `genesisToken`, `genesisTicker` and `genesisPool`. The site marks the coin as official from that record, never from its name: a second coin called TICKR under a different ticker is just a coin.\n\n## Why FUN has a pool, and the two buys after genesis\n\nChart sites price a pair by walking from its quote token to a dollar through pools, and they only trust a price they have seen trade into a wallet. FUN is worth the dollar it wraps, but without a pool those sites cannot show TICKR/FUN in dollars, and without a trade that lands in a wallet after the pool exists they show nothing at all. So FUN, like every redeemable name, runs its own FUN/USDG pool at one dollar from the moment it is invented ([05](./05-anchors.md)), and genesis ends with two more transactions from the deployer through Uniswap's canonical Universal Router, each with a fresh positive minimum from Uniswap's quoter: FUN bought through that pool into the deployer's wallet, then TICKR bought through FUN's pool and its own. The launch transaction itself, first buy included, is not a trade those sites count; the two after it are. Every coin launched on the site goes through the same two buys, from the create page, before it is shown as done. A confirmed sequence is not a listing: chart sites index on their own clock, and nothing here promises one.\n\n## Its address\n\nTICKR's address ends in `6942`, like every coin launched from the site. The genesis script grinds the salt against the deployed `LaunchDeployer`'s own prediction and refuses to launch if the predicted address does not end that way, so the suffix is checked before anything is sent.\n",
  "14-liquidity-lock.md": "# 14 · Liquidity lock\n\nEvery coin's liquidity is held by one contract, `LaunchLocker`, from the block it launches. This page says exactly what that contract holds, what it can do, and how to check both.\n\n## What the locker holds\n\n- The launch position of every coin: a Uniswap v4 position holding the entire supply, from the opening price to the end of the range. `LaunchSeeder` mints it straight to the locker in the launch transaction, so there is no moment when anyone else owns it.\n- The rounding remainder of the supply, the few units a one-sided position cannot absorb.\n\n`getLaunchedToken(token).lpTokenId` is the position, and `PositionManager.ownerOf(lpTokenId)` reads the locker's address for every launch.\n\n## What it can do\n\nTwo things. It can receive a position, through `onERC721Received`, and only from the Uniswap position manager, and only a fresh mint, never a position somebody already owns; the seeder checks that the locker owns the position before a launch is recorded. It can collect a position's fees, through `collectFees(token)`, which anyone may call at any time. A collection is a zero-liquidity decrease: Uniswap pays out what the position has earned and the principal is not touched. The proceeds are split by the terms frozen at launch, described in [06 fees](./06-fees.md).\n\n```solidity\nfunction pendingFees(address token) view returns (uint256 amount0, uint256 amount1);\nfunction collectFees(address token) returns (uint256 quoteOut, uint256 coinOut);\n```\n\n## What it cannot do\n\nNothing in the contract can move a position, reduce its liquidity, or send a coin or a quote anywhere except as the proceeds of a fee collection. Not for the creator, not for the protocol, not for the owner of the factory. The owner's full list of powers is in [09 risks](./09-risks.md), and the locker is not on it.\n\nThis is a property of the code, not a promise. The source has no such function, and a contract cannot gain one after deployment. It also cuts both ways: liquidity that cannot be pulled is liquidity that cannot be recovered, by anyone, for any reason.\n\n## What is not locked\n\nAnyone may add liquidity to a tickr pool and remove their own again. Only the launch position and the remainder are held by the locker. The two kinds of name differ here. A redeemable name's own pool is held by the wrapper itself behind `ManagedTickerHook`, and nobody else can add to or remove from that pool at all. A fixed-inventory name's pool has no hook, so anyone may add liquidity to it and remove their own again, exactly like a coin's pool; what they cannot touch is the name's own issuance, which is locked as described below. See [05 anchors](./05-anchors.md). A coin's price is set by everything in the pool, locked and not.\n\n## A fixed-inventory name's own supply\n\nHeld harder than a coin's, by a contract that is deliberately empty.\n\n`MarketTickerDeployer.create` mints the whole 500,000,000 issuance to itself and it leaves only into the name's\nopening position, which is minted straight to a `QuoteMarketLocker` deployed for that one name. That contract has\ntwo storage slots, a `record` function that can be called once, and `onERC721Received`. It has no transfer, no\napprove, no burn, no decrease, no rescue, no owner and no admin. Nothing was omitted by accident: any line that\ncould move the position is a line that could take the principal back out, so the absence of code is the guarantee.\n\nOne consequence to be aware of: **the name market's own fees are not collectable by anybody.** `LaunchLocker` can\ncollect a coin's fees because it has a function for it; this contract has none, so the fees a name's pool earns\naccrue in the position and stay there. They are not lost to a party, they are simply not withdrawable. If fee\ncollection is ever wanted it has to arrive as a separate reviewed contract that can show it touches accrued fees\nand never liquidity.\n\n`QuoteMarketCreated` records the locker's address and the position id, so both can be read without an indexer, and\n`QuoteMarketLocker.tokenId` and `.token` read them back off the locker itself.\n\n## How to check\n\n1. `Factory.getLaunchedToken(token)` gives `lpTokenId`, `tickLower`, `tickUpper` and `liquidity`.\n2. `PositionManager.ownerOf(lpTokenId)` returns the `LaunchLocker` address from the deployment record, [07 addresses](./07-addresses.md).\n3. The locker's verified source on the explorer has the two functions above and nothing that moves principal.\n",
  "15-launching-from-code.md": "# 15 · Launching from code\n\nRobinhood Chain, chain id 4663, RPC `https://rpc.mainnet.chain.robinhood.com`, explorer `https://robinhoodchain.blockscout.com`. Addresses are in [07 addresses](./07-addresses.md) and in the deployment record.\n\n## The pinned preview\n\nPin the opening economics with `expectedEconomics` so a change between your preview and your transaction reverts instead of launching at terms you did not see.\n\n```ts\nimport { parseEther, zeroAddress } from \"viem\";\nconst expectedEconomics = await client.readContract({ address: FACTORY, abi: factoryAbi, functionName: \"previewLaunchEconomics\", args: [0n, zeroAddress] });\nconst launchFee = await client.readContract({ address: FACTORY, abi: factoryAbi, functionName: \"launchFee\" });\nconst params = {\n  name: \"Example\", symbol: \"EXMPL\", logo: \"ipfs://bafk...\", description: \"What this is.\",\n  socials: { twitter: \"https://x.com/example\", telegram: \"\", discord: \"\", website: \"\", farcaster: \"\" },\n  creatorFeeRecipient: zeroAddress, // zero = the launching wallet\n  creatorTaxBps: 0,                 // 0..200\n  buybackEnabled: false,\n  expectedEconomics,\n  salt: randomBytes32,              // fresh every attempt; the site grinds it so the address ends in 6942\n};\n// launch and a 0.1 ETH dev buy, in one transaction. returns (token, poolId, tokensOut)\nawait wallet.writeContract({ address: ROUTER, abi: routerAbi, functionName: \"launchAndBuy\", args: [params, 0n, zeroAddress, parseEther(\"0.1\"), minTokensOut, account.address], value: launchFee + parseEther(\"0.1\") });\n// without a dev buy: Factory.launchToken(params, 0, pairToken) with value = launchFee. returns (token, poolId)\n```\n\n## Each quote asset\n\n- **ETH.** As above. The dev buy rides in `value` on top of the launch fee.\n- **USDG.** Approve the router for the dev buy amount and send `launchFee` as value.\n- **A Stock Token.** `StockQuoteLauncher.previewLaunch(0, stock)` gives `expectedEconomics`, then `launchWithStockQuote(params, 0, stock)` or `launchWithStockQuoteAndBuy(params, 0, stock, stockIn, minTokensOut)`.\n- **A tickr coin.** The same on `CoinQuoteLauncher`: `launchWithCoinQuote` and `launchWithCoinQuoteAndBuy`.\n- **Any token with a market.** `MarketQuoteLauncher.previewLaunch(0, token)` gives `expectedEconomics`, then `launchWithMarketQuote(params, 0, token)` or `launchWithMarketQuoteAndBuy(params, 0, token, tokenIn, minTokensOut)`. See [19](./19-market-quotes.md) for what counts as a market.\n- **A fixed-inventory name.** `MarketTickerLauncher`. `predictName(salt, symbol, decimals)` gives the name's address before it exists and `previewEconomics(configId, name)` the economics; `requiredDecimals()` is 6. The config id is **not** `0`: find the enabled config whose `baseFeeBps` is 82 and pass that one, because the launcher reverts with `NotTheFrozenFee` on anything else and the factory itself will not check for you. The coin must sort below its name here too. After the launch lands, send the same two listing buys, in the same order, each its own transaction. A name that already exists is launched under by address rather than by symbol; there is one name per symbol, as with redeemable names.\n\n- **A redeemable name.** `TickerLauncher.previewLaunch(symbol, 0)` gives the hash, then `launch(symbol, params, 0)` or `launchAndBuy(symbol, params, 0, usdgIn, minTokensOut)`. A new ticker costs `launchFee + NEW_TICKER_FEE()` as value, an existing one `launchFee`. The coin must sort below its name (`CoinNotFirst` otherwise): `predictTicker(symbol)` gives the name's address, which always starts with `0xF`, and `LaunchDeployer.predictToken` the coin's, so grind the salt until the coin's is lower; fifteen salts in sixteen are. After the launch lands, send the two listing buys, each its own transaction through Uniswap's canonical Universal Router with a fresh positive minimum: 0.0005 ETH of the name into the wallet, then 0.001 ETH of the coin; `contracts/script/lib/UniversalRouterBuy.sol` builds the exact calldata, see [12](./12-zap.md) and [05](./05-anchors.md). Both stages every time, also under a name that exists. Chart sites priced the reference's launch only after these.\n\n## Knowing the address first\n\nThe token address is known before the launch: `LaunchDeployer.predictToken(initiator, params, supply)`. The pool key follows from the address, so the pool id is known too. Before sending, simulate: every revert is a named error, listed in [18 events and errors](./18-events-and-errors.md).\n\n## Metadata\n\nLimits, enforced by `LaunchDeployer`: name 64 bytes, symbol 16, logo 8,192, description 2,048, each social link 256. Name and symbol cannot be empty. `logo` is a string stored on the coin. The site pins images to IPFS and stores `ipfs://...`; a plain `https://` link works too. Socials are stored as full links. None of it can be changed after launch.\n",
  "16-trading-from-code.md": "# 16 · Trading from code\n\nInside a coin's first five seconds a buy pays the snipe tax, taken on the way out of the pool: `Token.currentSnipeTaxBps(recipient)` is the rate at that second, and a quote that ignores it will not match what settles. The zap's `previewZap` already reports what the buyer keeps.\n\nEvery tickr pool is a Uniswap v4 pool with no hook. Uniswap's Universal Router, the v4 Quoter, aggregators and terminals trade it with the pool key in [17 reading state](./17-reading-state.md) and no tickr specific code. Their quotes already include the LP fee, because it is the pool's own.\n\n## One pool\n\n```solidity\n// LaunchSeeder: exact input on one pool. value = amountIn for a native input; approve the seeder for an ERC-20 input.\nfunction swapExactIn(PoolKey key, bool zeroForOne, uint256 amountIn, uint256 minOut, address recipient) payable returns (uint256 amountOut); // amountOut is what `recipient` received: for a coin inside its window, net of the snipe tax\nfunction swapExactInBounded(PoolKey key, bool zeroForOne, uint256 amountIn, uint256 minOut, address recipient, uint160 sqrtPriceLimitX96) payable returns (uint256 amountOut); // the same, stopping at a price: the pool takes input only until it reaches the limit, the rest comes back\n// two minimums: `swapExactIn`'s is a quantity, and a fill the pool cuts short because it ran dry must still deliver it or the call reverts whole.\n// `swapExactInBounded`'s is a price: calling it is consent to a fill that stops at the limit, and its `minOut` is held pro rata on the part the pool took, with the rest refunded\n```\n\nA buy is `zeroForOne = true` when the pair is currency0, which is always the case for ETH, since address zero sorts first.\n\n## From ETH into anything\n\nTo route from ETH into a coin quoted in something else, use `ZapRouter`, described in [12 buying with ETH](./12-zap.md). A route is a list of hops that ends at the coin's own pool: `kind` 0 for a v4 pool key, 1 for a v3 pool address, 2 for a wrap into a redeemable name. A fixed-inventory name has no wrap hop: route through its own USDG pool as an ordinary v4 hop (fee 500, spacing 10, no hook), and expect price impact on that leg as well as on the coin's. `previewZap` and `previewZapSell` revert with `Preview(quoteOut, amountOut)`, readable with `eth_call`.\n\n## Collecting fees and claiming\n\n```solidity\n// LaunchLocker\nfunction collectFees(address token) returns (uint256 quoteOut, uint256 coinOut);      // anyone may call\n// FeeEscrow\nfunction claim() returns (uint256);                                                 // ETH\nfunction claimToken(address token) returns (uint256);                               // any ERC-20\n```\n\nA collection splits by the terms frozen at launch, [06 fees](./06-fees.md). The coin side, every fee taken in the launched coin on sells, splits like the quote side: the creator's share and the tax go to the escrow in the coin, the protocol's and the club's shares are burned to `0x000000000000000000000000000000000000dEaD`; nobody is paid in the coin, so the burned supply of a coin is that address's balance. What a collection produced is in the `FeesCollected` event. Under a redeemable name a collection also books the coin's club volume for the current epoch: the quote fees collected divided by the pool fee rate, so buy volume in the quote, booked when collected and not when traded.\n\n## A coin under an invented name\n\nBuying it with ETH is the zap with a route through the ETH/USDG pool, then either the name's own pool (a v4 hop, fee 500, spacing 1, hooks = `ManagedTickerHook`, key from `TickerLauncher.poolKeyOf(ticker)`) or a wrap hop, then the coin's pool; selling it is the same route backwards. Paying in the name itself is one hop through the coin's own pool. The name is redeemable one for one either way: `ManagedTickerToken.mint` and `redeem` are one for one with USDG, outside any route (they refuse to run while the pool manager is unlocked), and the name's pool trades within two ticks of a dollar or not at all, with the pool's fee on top. See [12](./12-zap.md) for the listing buys.\n",
  "17-reading-state.md": "# 17 · Reading state\n\nEverything about a coin is on chain and readable without tickr's own code. This page lists the reads.\n\n## A launch\n\n```solidity\nstruct LaunchedToken {\n    address token;\n    address deployer;\n    address creatorFeeRecipient;\n    address pairToken;      // address(0) = native ETH\n    uint256 phantomQuote;   // the virtual quote reserve the pool opened with, raw units of the pair\n    uint24 poolFee;         // LP fee in pips: (base + creator tax) * 100, so 10000 for 1%\n    int24 tickSpacing;      // 10\n    int24 tickLower;\n    int24 tickUpper;\n    uint128 liquidity;\n    uint256 lpTokenId;      // the locked position, owned by LaunchLocker\n    uint16 creatorTaxBps;\n    bool buybackEnabled;\n    uint64 launchedAt;\n    bool exists;\n}\nfunction getLaunchedToken(address token) view returns (LaunchedToken);\nfunction poolKeyOf(address token) view returns (PoolKey);\nfunction poolIdOf(address token) view returns (bytes32);\nfunction getLaunchFeePolicy(address token) view returns (FeePolicy);\nfunction creatorFeeRecipientOf(address token) view returns (address);\nfunction ctoProposals(address token) view returns (address newRecipient, uint256 effectiveAt, uint256 expiresAt); // an owner proposal to move the fee wallet, zero when none: public until effectiveAt, executable by anyone until expiresAt\nfunction launchCount() view returns (uint256);\nfunction launchAt(uint256 i) view returns (address);\nfunction launchFee() view returns (uint256);           // 0.0005 ETH\nfunction maxCreatorTaxBps() view returns (uint256);    // 200\n```\n\n## Pool key and price\n\nThe two currencies sorted by address, the fee from the launch record, tick spacing 10, hook zero. `poolKeyOf` and `poolIdOf` return them; to compute them yourself:\n\n```ts\nimport { encodeAbiParameters, keccak256, zeroAddress } from \"viem\";\nconst [currency0, currency1] = BigInt(pairToken) < BigInt(token) ? [pairToken, token] : [token, pairToken];\nconst key = { currency0, currency1, fee: poolFee, tickSpacing: 10, hooks: zeroAddress };\nconst poolId = keccak256(encodeAbiParameters(\n  [{ type: \"address\" }, { type: \"address\" }, { type: \"uint24\" }, { type: \"int24\" }, { type: \"address\" }],\n  [key.currency0, key.currency1, key.fee, key.tickSpacing, key.hooks],\n));\n// spot price: slot0 lives at keccak256(abi.encode(poolId, uint256(6))) in the PoolManager\nconst slot = keccak256(encodeAbiParameters([{ type: \"bytes32\" }, { type: \"uint256\" }], [poolId, 6n]));\nconst word = await client.readContract({ address: POOL_MANAGER, abi: poolManagerAbi, functionName: \"extsload\", args: [slot] });\nconst sqrtPriceX96 = BigInt(word) & ((1n << 160n) - 1n);\n// price of currency1 in currency0 = (sqrtPriceX96 / 2^96)^2, raw units\n```\n\nMarket cap is that price times the supply of 1,000,000,000 coins, in the pair. The math behind the opening price is in [03 price and market cap](./03-curve-math.md).\n\n## Fees owed\n\n```solidity\n// LaunchLocker\nfunction pendingFees(address token) view returns (uint256 amount0, uint256 amount1); // in the pool's currency order\n// FeeEscrow\nfunction balanceOf(address recipient) view returns (uint256);                       // ETH\nfunction balanceOfToken(address recipient, address token) view returns (uint256);   // any ERC-20\n```\n\n## Redeemable names\n\n```solidity\n// TickerLauncher\nfunction tickerFor(string symbol) view returns (address);   // zero if not invented yet\nfunction isTicker(address) view returns (bool);\nfunction tickerCount() view returns (uint256);\nfunction tickerAt(uint256 i) view returns (address);\nfunction pairsOf(address ticker) view returns (address[]);\nfunction captainOf(address ticker, uint256 epoch) view returns (address);   // counts double in that epoch's split: the founder's coin while it trades, else the top coin\nfunction topOf(address ticker, uint256 epoch) view returns (address);       // the most booked volume under the ticker in the epoch\nfunction NEW_TICKER_FEE() view returns (uint256);           // 0.0015 ETH\nfunction INVENTORY_FLOOR() view returns (uint256);          // 10,000e6: the offer of every name's pool at rest\nfunction predictTicker(string symbol) view returns (address); // where the name lives, invented or not\nfunction poolKeyOf(address ticker) view returns (PoolKey);   // the name's own pool against USDG\n// ManagedTickerToken: a one for one wrapper with its own pool\nfunction counter() view returns (IERC20);                   // USDG\nfunction mint(uint256 amount, address to);                  // only while the pool manager is locked, so not from inside a route\nfunction redeem(uint256 amount, address to);\nfunction poolKey() view returns (PoolKey);                  // WRAPPER/USDG, fee 500, spacing 1, hooks = ManagedTickerHook\nfunction accounting() view returns (uint256 backing, uint256 circulation); // backing covers circulation, always\nfunction circulatingSupply() view returns (uint256);        // what is out: totalSupply less the wrapper's own inventory\nfunction usableBacking() view returns (uint256);            // backing less what the bridge position keeps\nfunction inventoryCapacity() view returns (uint256);        // the most one buy through the pool can take: floor + 4 x circulation\nfunction hook() view returns (address);\n// ManagedTickerHook\nfunction tokenOf(bytes32 poolId) view returns (address);     // the wrapper behind a pool, zero for any other pool\nfunction issuer() view returns (address);                   // the ticker launcher\n```\n\n`totalSupply()` of a wrapper counts its own unsold inventory, which is neither owed to anyone nor backed until sold; read `circulatingSupply()` for what is out.\n\nThe club: `pot(ticker, epoch, coin)`, `volumeOf(coin, epoch)` (booked when the coin's fees are collected, as the quote fees collected divided by the pool fee rate: buy volume in the quote, in the epoch of the collection), `clubVolume(ticker, epoch)`, `claimable(member, payers, epoch)`, `claimClub(member, payers, epoch)`, `sweepDeadPot(coin, epoch)`, `currentEpoch()`; epochs are thirty days. See [05 redeemable names](./05-anchors.md).\n\n## Which kind of name a quote asset is\n\nDo not infer it from the token. Ask the registry, which answers by provenance: which issuer deployed the name.\n\n```solidity\nQuoteRegistry.kindOf(address name) returns (uint8)\n// 0 unknown          neither issuer made it, or it predates the registry\n// 1 redeemable       TickerLauncher: a one-for-one USDG wrapper, mint and redeem\n// 2 fixed inventory  MarketTickerDeployer: 500,000,000 at 6 decimals, its own market, no redeem\n```\n\n`0` is not an error and must not be treated as one. FUN, the name TICKR is priced in, returns `0` because it\npredates the registry, and everything on chain that reads this treats an unclassified name the way it always\ndid: as a redeemable wrapper, which is what FUN is. `TickerLauncher.isTicker(addr)` still answers the same\nquestion for the redeemable kind alone and is what the site keys its pricing on today.\n\nPricing consequence worth knowing before you integrate: a redeemable name is worth one USDG by construction, so\nyou may price it at a dollar. A fixed-inventory name is worth whatever its own USDG pool says, so you must read\nthat pool. Pricing one at a dollar is wrong and will misreport every coin quoted in it.\n\n## The coin itself\n\n```solidity\n// Token\nfunction logo() view returns (string);\nfunction description() view returns (string);\nfunction socials() view returns (Socials);\nfunction liquidityPool() view returns (bytes32);      // its pool id\nfunction owner() view returns (address);              // always zero: there is no owner\nfunction contractURI() view returns (string);         // ERC-7572 JSON, inline: name, symbol, description, image, links\nfunction launchedAt() view returns (uint64);\nfunction SNIPE_WINDOW() view returns (uint256);       // 5 seconds\nfunction currentSnipeTaxBps(address recipient) view returns (uint256);   // the snipe tax a buy pays right now, zero for the launch's own wallets and after the window\nfunction launchedBlock() view returns (uint64);\nfunction protectionEndsAtBlock() view returns (uint256);          // the launch block plus three: the first block with no caps\nfunction remainingBuy(address wallet) view returns (uint256);    // coins the wallet may still buy under the 5.5% cap; max when no cap applies, zero in the launch block\nfunction remainingHold(address wallet) view returns (uint256);   // coins the wallet may still receive, bought or sent, under the 5% cap; same rules\n// LaunchDeployer\nfunction MAX_NAME() view returns (uint256);           // 64, then MAX_SYMBOL 16, MAX_LOGO 8192, MAX_DESCRIPTION 2048, MAX_SOCIAL 256\n```\n\n## Any token with a market\n\n```solidity\n// MarketQuoteLauncher\nstruct Market { address pool; address counter; uint24 fee; uint256 depth; uint256 inBand; }\nfunction bestMarket(address quote) view returns (Market);   // reverts with the reason when there is none\nfunction minDepth(address counter) view returns (uint256);  // the floor, total and within the band\nfunction BAND_BPS() view returns (uint256);                 // 500\n```\n\n## The buyback treasury\n\n```solidity\n// BuybackTreasury: the protocol's fee share, buying and burning TICKR\nfunction earmarkedUsdg() view returns (uint256);   // dollars set aside for buys\nfunction totalUsdgSpent() view returns (uint256);\nfunction totalTickrBurned() view returns (uint256);\nfunction lastBuyAt() view returns (uint256);\nfunction nextBuyAt() view returns (uint256);        // zero until the first buy\nfunction previewBuy() view returns (uint256 usdgIn, uint256 minTickrOut);\nfunction official() view returns (address tickr, address fun);\nfunction collect(address[] tokens) returns (uint256 usdgTotal, uint256 toTeam, uint256 earmarked);\nfunction buy() returns (uint256 usdgIn, uint256 tickrOut);\n// events\nevent Collected(uint256 usdgTotal, uint256 toTeam, uint256 earmarked);\nevent Forwarded(address indexed asset, uint256 amount);       // an unconvertible asset sent whole to the team\nevent BoughtAndBurned(uint256 usdgIn, uint256 tickrOut, address indexed caller);\n// constants: MAX_IMPACT_BPS 300, MIN_INTERVAL 10 minutes, MAX_TRANCHE_BPS 500, SHARE_DELAY 3 days\nfunction buybackShareBps() view returns (uint16);       // the burn share in force, 5000 at the start; only ever raised\nfunction pendingShareBps() view returns (uint16);       // a proposed raise waiting out its delay, zero when none\nfunction shareEffectiveAt() view returns (uint256);     // when the pending raise may be applied\nfunction proposeBuybackShare(uint16 bps);               // the factory owner only; higher than the current share, at most 10000\nfunction applyBuybackShare();                           // anyone, once shareEffectiveAt has passed\n// launcher(): the ticker launcher the treasury is bound to; zero until its first collect or buy\n```\n\n## The site's own endpoints\n\n`/api/pair/<address>` describes any address as a possible quote asset: registry match, lookalikes, pools. `/api/pin` pins an image or metadata to IPFS for the create page. `/llms.txt` and `/llms-full.txt` mirror these docs; `/docs/<slug>.md` serves each page as markdown.\n",
  "18-events-and-errors.md": "# 18 · Events and errors\n\n## Events\n\n```solidity\n// Factory\nevent TokenLaunched(address indexed token, bytes32 indexed poolId, address indexed deployer, address pairToken, uint256 launchConfigId, uint24 poolFee, uint256 phantomQuote);\nevent LaunchPositionLocked(address indexed token, uint256 lpTokenId, int24 tickLower, int24 tickUpper, uint128 liquidity);\n// the owner's public notice before a fee wallet moves without its holder: three days of notice, then a three day window\nevent CreatorFeeRecipientChangeProposed(address indexed token, address indexed newRecipient, uint256 effectiveAt, uint256 expiresAt);\nevent CreatorFeeRecipientUpdated(address indexed token, address indexed newRecipient);\nevent FeeClubUpdated(address club);\n// LaunchSeeder\nevent LaunchSeeded(address indexed token, bytes32 indexed poolId, uint256 tokenId, int24 tickLower, int24 tickUpper, uint128 liquidity);\nevent DollarPoolSeeded(address indexed wrapper, bytes32 poolId, uint256 usdgIn, uint256 wrapperIn, uint256 tokenId);\n// LaunchLocker\nevent PositionLocked(address indexed positionManager, uint256 indexed tokenId);\n// creatorCoin is the creator's share of the coin side plus the whole tax part, credited in the escrow; burnedCoin is the\n// protocol's and the club's shares of it, sent to the dead address\nevent FeesCollected(address indexed token, uint256 quoteCollected, uint256 coinCollected, uint256 protocolQuote, uint256 creatorQuote, uint256 clubQuote, uint256 creatorCoin, uint256 burnedCoin);\n// routers and launchers\nevent FirstBuy(address indexed token, uint256 quoteIn, uint256 tokensOut);\nevent TickerCreated(address indexed ticker, string symbol, address indexed by);\nevent Launched(address indexed token, bytes32 indexed poolId, address indexed ticker);\n// BuybackTreasury: the burn share only goes up. a proposal by the factory owner, then anyone applies it after the delay\nevent BuybackShareProposed(uint16 bps, uint256 effectiveAt);\nevent BuybackShareRaised(uint16 bps);\n// PoolManager, filtered by poolId: the input currency is negative and includes the fee, the output positive\nevent Swap(bytes32 indexed id, address indexed sender, int128 amount0, int128 amount1, uint160 sqrtPriceX96, uint128 liquidity, int24 tick, uint24 fee);\n```\n\n## Selectors\n\nComputed from the compiled ABIs by `web/scripts/gen-selectors.mjs` every time the site syncs the docs, so the hashes are the code's, not a transcription. `topic0` is what an `eth_getLogs` filter takes; an error's selector is the first four bytes of its revert data. One signature can live in several contracts; the last column lists them all.\n\n<!-- selectors:start -->\n<!-- generated by web/scripts/gen-selectors.mjs from contracts/out. do not edit by hand. -->\n\n### Events\n\n| event | topic0 | emitted by |\n| --- | --- | --- |\n| `AnchorRegistered(address,string,uint8,address)` | `0xb795aa27a856d41cc57e3c8c50c22cb3e0a53618ea9af00ddc8a96f40c4d2f08` | AnchorRegistry |\n| `AnchorStatus(address,bool)` | `0xc7eeae902570c875dbc35081a2294ff8d21360be748b671052bd3751a6ee3a83` | AnchorRegistry |\n| `Approval(address,address,uint256)` | `0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925` | Token, ManagedTickerToken |\n| `AwaitingRegistration(address,uint256)` | `0x7f58826b3f0c7f6de8a55794cd5a9efa26bf3541819a4aa9a8ac7c797d6e0b36` | BuybackTreasuryV2 |\n| `BoughtAndBurned(uint256,uint256,address)` | `0x1b6fe3d614107093562b62b9236e265cac820f430060c5eb674a70824a7435db` | BuybackTreasury, BuybackTreasuryV2 |\n| `BuybackHeld(address,uint256)` | `0x591812a4a303f0f2ad9a6660b614b379493bb8cf1d1e66d4c0290c1f52ea473b` | BuybackTreasuryV2 |\n| `BuybackReturned(address,uint256)` | `0xd56dee4958c0c026d1e0cd9f422df88ab2d497f94abe87d71b322a1adc3b2922` | BuybackTreasuryV2 |\n| `BuybackShareProposed(uint16,uint256)` | `0x25f0057b4f4e56b85281b097467926d4f104d1721347d3edc384a4e251d94f9b` | BuybackTreasury, BuybackTreasuryV2 |\n| `BuybackShareRaised(uint16)` | `0x3c027b913b338f2a029e8d3b9d6afcfc378bededed0ea3e00035157028840b91` | BuybackTreasury, BuybackTreasuryV2 |\n| `Claimed(address,uint256)` | `0xd8138f8a3f377c5259ca548e70e4c2de94f129f5a11036a15b69513cba2b426a` | FeeEscrow |\n| `ClaimedToken(address,address,uint256)` | `0xdbc1ea3a8459e4c7e11fb385b52bbb5cc8c8ab85eec5d883ac9aa78c171f5141` | FeeEscrow |\n| `ClubClaimed(address,uint256,address,address,uint256)` | `0x659005b82109aa113441f2c563c06b60aa77aa7ffb11627b095cc2c247564623` | TickerLauncher |\n| `ClubFee(address,uint256,address,uint256)` | `0x0877294740802b7587101f4754015f9e6694c9ec53849aceffd0dca93dab6d81` | TickerLauncher |\n| `CoinQuoteLaunched(address,bytes32,address,address,uint256,uint256)` | `0xb5210e97fb7d72ef8834f69f62bde6ca74816fe42ffbad27e7484f6db93f1e79` | CoinQuoteLauncher |\n| `Collected(uint256,uint256,uint256)` | `0x6059b72f21911d96e92ebeb103aa1162802c8b875d60921f9db35ec8967fc7f6` | BuybackTreasury, BuybackTreasuryV2 |\n| `CreatorFeeRecipientChangeProposed(address,address,uint256,uint256)` | `0x22b336cc3c163283ca94b8a02d1107b4485a820fa77cc12ea205640452d37b5e` | Factory |\n| `CreatorFeeRecipientUpdated(address,address)` | `0x2cc664e1ac1e2d05c0d4637bb63ec8189113b6ac39276be8977e26216a8cdd19` | Factory |\n| `Credited(address,uint256)` | `0x9c9e0cdb1670e88302408e5b941dc636f63a3293204694bb68910fcf3d9305f3` | FeeEscrow |\n| `CreditedToken(address,address,uint256)` | `0x2ad02ae08ced0d667a621b3a5a2bd7d9574d95a1567d5caca94845c7c03f5639` | FeeEscrow |\n| `DeadPotSwept(address,uint256,address,address,uint256)` | `0xfccecb69fffb131a589af1027fdd1fe2324490fbabc8dc0d4c18eaf8641bd4d2` | TickerLauncher |\n| `Deposited(address,address,uint256)` | `0x8752a472e571a816aea92eec8dae9baf628e840f4929fbcc2d155e6233ff68a7` | BuybackVault |\n| `FeeClubUpdated(address)` | `0x9c5eec170472c99337d91c4a5e9d03011b0f990a40b636d2f821b2f7618daa0c` | Factory |\n| `FeePolicyUpdated((address,uint16,uint16,uint16,uint16,address,uint16,uint16))` | `0x3305ad2f721aaaa69e560f58a2c1224a88ae25a6b88132bbdad2a63404e6916d` | Factory |\n| `FeesCollected(address,uint256,uint256,uint256,uint256,uint256,uint256,uint256)` | `0xa1f59c4198b06eed65ba9c4eb11e1040d74f53818977958b489376f7cb851c0e` | LaunchLocker |\n| `FirstBuy(address,uint256,uint256)` | `0x4c71b8a31a6b3e892579c501fcaecc04e992f4f430a7f4772ac5db32da85ec63` | LaunchAndBuyRouter, TickerLauncher, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher |\n| `Forwarded(address,uint256)` | `0x6f1deddfc28100c291fae8f1064e4a91e844f0841993bb8fba9a913c3b801d80` | BuybackTreasury, BuybackTreasuryV2 |\n| `Initialize(bytes32,address,address,uint24,int24,address,uint160,int24)` | `0xdd466e674ea557f56295e2d0218a125ea4b4f0f6f3307b95f85e6110838d6438` | Uniswap PoolManager |\n| `LaunchConfigAdded(uint256)` | `0xedd96c570c6e5ef9add0378e59df53579a283889dc5dab6440ef6eca2ee6c8ce` | Factory |\n| `LaunchConfigUpdated(uint256)` | `0x2f8ba78ae68cfd0c82c7756c540eaf4eead3341aef9ccebcb91d546bff10d62b` | Factory |\n| `Launched(address,bytes32,address)` | `0x7d7973b0bce0568727967767ebffbb956d03b099cfaeb0d8e6e338d2f0a03787` | TickerLauncher |\n| `LauncherBound(address)` | `0x3ae987fd9641b8f46247990290ac31493ccf09c4e52cae5a61566983e46dc69e` | BuybackTreasury, BuybackTreasuryV2 |\n| `LaunchFeeUpdated(uint256)` | `0xc799be5eb19a1a6d6ba7368d21e2bc367c8a335e4a07cd3d954482e6f714d3c5` | Factory |\n| `LaunchPositionLocked(address,uint256,int24,int24,uint128)` | `0x1699dadac90fce1f13f689ddb1cf88b07a25fd2339468d846b5ea6b22704786a` | Factory |\n| `LaunchSeeded(address,bytes32,uint256,int24,int24,uint128)` | `0xe777d7555b919fda019704ee6e89c0a5781e7bd21edb97a02a0890d6d699ef0b` | LaunchSeeder |\n| `Maintained(uint256,uint256,uint256,uint256)` | `0xa931cc0d5fef691941b395c737ad6fb723ce72ed847121df18f956b6079bd96b` | ManagedTickerToken |\n| `MaintenanceSkipped(bytes4)` | `0x2db4857148d10584800878afc58bab39d2df889e4a58ca4f14fa21d1dae41973` | ManagedTickerToken |\n| `MarketLaunch(address,address,address,bool)` | `0xef2fd5b75097f0bd454ae6de78642a5b03d5f0d339346199f14061b11fd6892c` | MarketTickerLauncher |\n| `MarketQuoteLaunched(address,bytes32,address,address,address,uint256,uint256)` | `0x29f7311eb056f66ba342c1dd302c83730e9bb314dfff45a2271e8a89333c29df` | MarketQuoteLauncher |\n| `MaxStalenessUpdated(uint256)` | `0x64a4703c7c168827058126cbd2e71d8d0f026afa821e7dff1480173dffdd3895` | StockQuoteLauncher |\n| `MaxTermsAheadSet(uint256)` | `0xc2826301bc7c9f1c31e8c6909ea903935f3daa41bc1bab52f3e2c5b11b174502` | BuybackTreasuryV2 |\n| `MinDepthUpdated(address,uint256)` | `0x4855ecd1c83ead9615b46d104300d3ddfc6d1d35ff0076ab229bf3c76544675d` | MarketQuoteLauncher |\n| `MinRateSet(address,bool,uint256)` | `0x870a711d451f7c5a46d3dfb9a159d897a9f0939b39890a6fb1bea0a26849e6df` | BuybackTreasuryV2 |\n| `Minted(address,address,uint256)` | `0x9d228d69b5fdb8d273a2336f8fb8612d039631024ea9bf09c424a9503aa078f0` | ManagedTickerToken |\n| `ModifyLiquidity(bytes32,address,int24,int24,int256,bytes32)` | `0xf208f4912782fd25c7f114ca3723a2d5dd6f3bcc3ac8db5af63baa85f711d5ec` | Uniswap PoolManager |\n| `NameReserved(string,bool)` | `0x3991f588a604787469eff195387d64a0412af48ef404233dadc122594114f38f` | AnchorRegistry |\n| `NotConverted(address,uint256,bytes)` | `0xc4e78006a8e01f1d2372eda029ea9639e658411bd75b777a8eb75f1c8d9ec3d6` | BuybackTreasuryV2 |\n| `OwnershipTransferred(address,address)` | `0x8be0079c531659141344cd1fd0a4f28419497f9722a3daafe3b4186f6b6457e0` | Factory, AnchorRegistry, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, BuybackVault |\n| `OwnershipTransferStarted(address,address)` | `0x38d16b8cac22d99fc7c124b9cd0de2d3fa1faef420bfe791d8c362d765e22700` | Factory, AnchorRegistry, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, BuybackVault |\n| `PairRegistrarUpdated(address,bool)` | `0xe6c0fba2f3535e9a5d84e056a4c0ad64d9fc1d7040770dc47ed1090e64491116` | Factory |\n| `PairTokenApproved(address,uint256,uint8)` | `0x1d5a4cb2ed187e012b376c379f93b8be85c53df9e13d9d6b373d321ad09493e6` | Factory |\n| `PairTokenRevoked(address)` | `0x01d82fe5117ed9433136541c9323e3ed69942ed6b5537ee00f01d04983a8679c` | Factory |\n| `PositionLocked(address,uint256)` | `0x2cabb2a2973327d5863ceb4707e9441851243897e86d587ee35943599752eb54` | LaunchLocker |\n| `QuoteMarketCreated(address,address,bytes32,address,uint256,uint256,int24,int24,uint24,int24)` | `0x7f9aadfa73ac5d753756eea6d568d049161eca89f33562032544589023e37b26` | MarketTickerDeployer |\n| `QuoteRecorded(address,uint8,address)` | `0x97f1b4e3ecfe78d1b6fab4494c1e8e6701e0fe74e87c98c92931fd9c97744f5a` | QuoteRegistry |\n| `Recentered(uint256,uint256,bool)` | `0x7ba9c7048246d156f23f799b728c18d3226313c76df961a2170c960edfce1eb0` | ManagedTickerToken |\n| `Redeemed(address,address,uint256)` | `0x27d4634c833b7622a0acddbf7f746183625f105945e95c723ad1d5a9f2a0b6fc` | ManagedTickerToken |\n| `Registered(address,bytes32)` | `0xb8142d42f05d95abf0a6570799774d59276e49ea32a04d9a4ec316ea4a6886bc` | ManagedTickerHook |\n| `Released(address,address,address,uint256)` | `0xefb6092aa61d438ccc04ee833a4f614dfde5a125bf25f38811be9a9156676134` | BuybackVault |\n| `SnipeTaxed(address,uint256,uint256)` | `0x8a9686f3b65d9268c33d1f589d165576aac5b9aee6222b4d34cde8bdc40dc19d` | Token |\n| `StockQuoteLaunched(address,bytes32,address,uint256,uint256)` | `0xadfbfdadb3195db79b43be989a1308d788c329de3b42ea03daac49659eb0e289` | StockQuoteLauncher |\n| `StrategyUpdated(address)` | `0x4da9c22c924692646a21cf1f423781ae3285198dc22e8a6912835d3272b90b3c` | BuybackVault |\n| `Swap(bytes32,address,int128,int128,uint160,uint128,int24,uint24)` | `0x40e9cecb9f5f1f1c5b9c97dec2917b7ee92e57ba5563708daca94dd84ad7112f` | Uniswap PoolManager |\n| `TargetRaiseUpdated(address,uint256)` | `0xfe9c076584bb0e1441fd67cac5a630f1c64d813da88a0ec15b619c0c60409b53` | CoinQuoteLauncher, MarketQuoteLauncher |\n| `TargetRaiseUpdated(uint256)` | `0x6699f49b8c3d0c720ee0f0b99924f117a62d7588461f64e9cf287498a5147787` | StockQuoteLauncher |\n| `TickerCreated(address,string,address)` | `0xa2554fdf89f52c818624566c35170d0acb705d4aaa9c8389c7d47838c7a18d10` | TickerLauncher |\n| `TickerLauncherUpdated(address)` | `0x0fe434542b42c1352b053543bf6bd87cc3ea836c276eb05c8e9fe44f9ba769d1` | Factory |\n| `TickerReserved(string,bool)` | `0x52cec17453a6a79bbd62df80b689eb80cdef3d0d28d4a668447631efdb280940` | AnchorRegistry |\n| `TokenLaunched(address,bytes32,address,address,uint256,uint24,uint256)` | `0xb6aa2732938645ba6006aecc1a69e2e2c105447698bfeceb2fd347532ae40954` | Factory |\n| `Transfer(address,address,uint256)` | `0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef` | Token, ManagedTickerToken |\n| `Zapped(address,address,address,uint256,uint256,uint256)` | `0xe91c019e21e619ffcc1911755ca8385b08392696a46dbfc31b4cc69804161ad2` | ZapRouter, MarketZapRouter |\n| `ZapSold(address,address,address,uint256,uint256,uint256)` | `0xcae331be1761c82e841551cd62fb652b07ae6f51962c767cfc8e6d4b11fe4a8f` | ZapRouter, MarketZapRouter |\n\n### Errors\n\n| error | selector | declared in |\n| --- | --- | --- |\n| `AlreadyHolding()` | `0xec55fe3d` | QuoteMarketLocker |\n| `AlreadyRecorded(address,uint8)` | `0xdd361204` | QuoteRegistry |\n| `AlreadyRegistered()` | `0x3a81d6fc` | ManagedTickerHook |\n| `BadName()` | `0x153736cd` | LaunchDeployer |\n| `BadPath()` | `0x64325c91` | ZapRouter, MarketZapRouter |\n| `BadPrice()` | `0xfd1ee349` | StockQuoteLauncher |\n| `BadSymbol()` | `0x73a289b0` | LaunchDeployer, TickerLauncher |\n| `BadValue()` | `0x0bba69fb` | LaunchSeeder, LaunchAndBuyRouter, TickerLauncher, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, ZapRouter, MarketZapRouter |\n| `CapacityExceeded(uint256,uint256)` | `0xc28f738e` | ManagedTickerToken |\n| `CoinNotFirst(address,address)` | `0x587fe89d` | TickerLauncher, MarketTickerLauncher |\n| `CreatorTaxTooHigh()` | `0x9ad465dc` | Factory |\n| `DeadlineTooFar(uint256,uint256)` | `0x657526de` | BuybackTreasuryV2 |\n| `DecimalsMismatch(address,uint8,uint8)` | `0x85d6e41f` | MarketTickerLauncher |\n| `EmptyMetadata()` | `0xae921357` | LaunchDeployer |\n| `EmptySymbol()` | `0x198c27f6` | TickerLauncher |\n| `EpochOpen()` | `0xa2e041e8` | TickerLauncher |\n| `ERC20InsufficientAllowance(address,uint256,uint256)` | `0xfb8f41b2` | Token, ManagedTickerToken |\n| `ERC20InsufficientBalance(address,uint256,uint256)` | `0xe450d38c` | Token, ManagedTickerToken |\n| `ERC20InvalidApprover(address)` | `0xe602df05` | Token, ManagedTickerToken |\n| `ERC20InvalidReceiver(address)` | `0xec442f05` | Token, ManagedTickerToken |\n| `ERC20InvalidSender(address)` | `0x96c6fd1e` | Token, ManagedTickerToken |\n| `ERC20InvalidSpender(address)` | `0x94280d62` | Token, ManagedTickerToken |\n| `Expired()` | `0x203d82d8` | ZapRouter, MarketZapRouter |\n| `Insolvent(uint256,uint256)` | `0x1f2c89f0` | ManagedTickerToken |\n| `InsufficientLiquidity()` | `0xbb55fd27` | ZapRouter, MarketZapRouter |\n| `InsufficientMaintenanceSurplus()` | `0xf797980c` | ManagedTickerToken |\n| `InvalidPolicy()` | `0xd06b96b1` | Factory |\n| `InventoryNotFullyPlaced(uint256,uint256)` | `0x2d67846b` | MarketTickerDeployer |\n| `IsTheCounter(address)` | `0xbb7aeb8b` | QuoteRegistry |\n| `LaunchBlock()` | `0x062b673f` | Token |\n| `LaunchConfigDisabled()` | `0xa8b63076` | Factory |\n| `LaunchEconomicsMismatch()` | `0x3cba147e` | Factory |\n| `LaunchFeeNotPaid()` | `0x7e6d78a5` | Factory |\n| `MarketExhausted(address,address)` | `0x753098a4` | MarketZapRouter |\n| `MarketNotPrimed(address)` | `0xf43cba15` | BuybackTreasuryV2 |\n| `MetadataTooLong()` | `0x85b8e2f4` | LaunchDeployer |\n| `MultipleBridgeVisits()` | `0x2ccba773` | ManagedTickerToken |\n| `NameReserved()` | `0x577a3cce` | Factory |\n| `NoFeed()` | `0x7e1f7c28` | StockQuoteLauncher |\n| `NoMarket()` | `0x78dc030c` | MarketQuoteLauncher |\n| `NoMarket(address)` | `0x2df59b68` | BuybackTreasuryV2 |\n| `NoPolicy(address)` | `0x10beed5b` | BuybackTreasuryV2 |\n| `NotAContract(address)` | `0x8a8b41ec` | QuoteRegistry |\n| `NotALaunchPool(address)` | `0xa120caf9` | LaunchLocker |\n| `NotAMint()` | `0x98bc7fb5` | LaunchLocker |\n| `NoTargetRaise()` | `0x899d778d` | StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher |\n| `NotAWrapper(address,uint8)` | `0x2470d467` | MarketZapRouter |\n| `NotCreatorFeeRecipient()` | `0xb9f93944` | Factory |\n| `NoTerms()` | `0x55f17ace` | BuybackTreasuryV2 |\n| `NotFactoryOwner()` | `0x428e0b92` | BuybackTreasury, BuybackTreasuryV2 |\n| `NotFeeSource()` | `0x70154a5e` | TickerLauncher |\n| `NotFromEscrow()` | `0xbe81426f` | BuybackTreasury, BuybackTreasuryV2 |\n| `NothingEarmarked()` | `0xdbded5a4` | BuybackTreasury, BuybackTreasuryV2 |\n| `NothingLocked(address)` | `0xd9e7f38a` | LaunchLocker |\n| `NothingPending()` | `0x175c1aea` | BuybackTreasury, BuybackTreasuryV2 |\n| `NothingPlaced()` | `0x51a5de9c` | MarketTickerDeployer |\n| `NothingToBuy()` | `0xb2f53681` | BuybackTreasury, BuybackTreasuryV2 |\n| `NothingToSweep()` | `0x351261fc` | TickerLauncher |\n| `NotHook()` | `0xc72cfc38` | TickerLauncher |\n| `NotInitialized()` | `0x87138d5c` | ManagedTickerToken |\n| `NotIssuer()` | `0x54ec5063` | ManagedTickerHook, ManagedTickerDeployer, MarketTickerDeployer |\n| `NotOfficialStockToken()` | `0x63a68476` | StockQuoteLauncher |\n| `NotOneSided()` | `0xb3ac93d1` | LaunchSeeder |\n| `NotOwner()` | `0x30cd7471` | BuybackTreasuryV2 |\n| `NotPoolManager()` | `0xae18210a` | ManagedTickerHook |\n| `NotPositionManager()` | `0x20fdc658` | LaunchLocker |\n| `NotRegistered(address)` | `0xbfc6c337` | BuybackTreasuryV2 |\n| `NotRegistrar()` | `0x2ad95351` | Factory |\n| `NotTheCounter(address)` | `0x7040536e` | MarketTickerLauncher |\n| `NotTheFrozenFee(uint256,uint16)` | `0xde7c0d0d` | MarketTickerLauncher |\n| `NotTheWrapper()` | `0x627da710` | ManagedTickerHook |\n| `NotUnderTicker()` | `0x5859c16a` | TickerLauncher |\n| `NotWhitelisted()` | `0x584a7938` | Factory |\n| `NotYetLaunched()` | `0x8f7ce531` | BuybackTreasury, BuybackTreasuryV2 |\n| `OnlyFactory()` | `0x0c6d42ae` | LaunchDeployer, LaunchSeeder |\n| `OnlyPool()` | `0x4b602735` | ZapRouter, MarketZapRouter |\n| `OnlyPoolManager()` | `0xf655705d` | LaunchSeeder, ZapRouter, MarketZapRouter |\n| `OwnableInvalidOwner(address)` | `0x1e4fbdf7` | Factory, AnchorRegistry, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, BuybackVault |\n| `OwnableUnauthorizedAccount(address)` | `0x118cdaa7` | Factory, AnchorRegistry, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, BuybackVault |\n| `PairTokenDecimalsMismatch()` | `0xe16fd21f` | Factory |\n| `PairTokenNotApproved()` | `0x49285dfb` | Factory |\n| `PartialFill()` | `0xd964f528` | ManagedTickerToken |\n| `PoolAlreadyExists()` | `0x03119322` | LaunchSeeder |\n| `PoolExists()` | `0xf48e3c26` | MarketTickerDeployer |\n| `PositionNotLocked()` | `0xd58f9686` | LaunchSeeder |\n| `Preview(uint256,uint256)` | `0x762a4020` | ZapRouter, MarketZapRouter |\n| `QuoteDecimals()` | `0x7eccfb48` | MarketQuoteLauncher |\n| `QuoteIsAnchor()` | `0xa3b8e31e` | MarketQuoteLauncher |\n| `QuoteLaunchedHere()` | `0xf48e4bfb` | MarketQuoteLauncher |\n| `QuoteNotGraduated()` | `0xb24fcfe0` | CoinQuoteLauncher |\n| `QuoteNotLaunchedHere()` | `0xce99f096` | CoinQuoteLauncher |\n| `QuotePriceUnavailable()` | `0x12ecbbe8` | CoinQuoteLauncher, MarketQuoteLauncher |\n| `QuoteTooDeep()` | `0x4e6ebcc8` | CoinQuoteLauncher |\n| `RateTooLow(uint256,uint256)` | `0x8e03f3e5` | BuybackTreasuryV2 |\n| `ReentrancyGuardReentrantCall()` | `0x3ee5aeb5` | Factory, LaunchSeeder, LaunchLocker, FeeEscrow, LaunchAndBuyRouter, TickerLauncher, ManagedTickerToken, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, ZapRouter, BuybackTreasury, MarketZapRouter, BuybackTreasuryV2 |\n| `RefundFailed()` | `0xf0c49d44` | LaunchSeeder |\n| `SafeERC20FailedOperation(address)` | `0x5274afe7` | Factory, LaunchDeployer, LaunchSeeder, LaunchLocker, FeeEscrow, LaunchAndBuyRouter, TickerLauncher, ManagedTickerToken, StockQuoteLauncher, CoinQuoteLauncher, MarketQuoteLauncher, ZapRouter, BuybackTreasury, BuybackVault, MarketTickerDeployer, MarketZapRouter, BuybackTreasuryV2 |\n| `ShareNotHigher()` | `0xe4354168` | BuybackTreasury, BuybackTreasuryV2 |\n| `ShareTooHigh()` | `0x2a07fc4c` | BuybackTreasury, BuybackTreasuryV2 |\n| `Slippage()` | `0x7dd37f70` | LaunchSeeder, ZapRouter, MarketZapRouter |\n| `StalePrice()` | `0x19abf40e` | StockQuoteLauncher |\n| `SymbolTooLong()` | `0x1124f78b` | TickerLauncher |\n| `TermsExpired(uint256,uint256)` | `0x5400f80b` | BuybackTreasuryV2 |\n| `TermsWeakerThanPolicy(address,uint256,uint256)` | `0xf3a558b0` | BuybackTreasuryV2 |\n| `TickerReserved()` | `0x8bf25255` | Factory, TickerLauncher |\n| `TimelockExpired()` | `0x7a6fcaa6` | Factory |\n| `TimelockNotElapsed()` | `0x6677a596` | Factory |\n| `TooEarly(uint256)` | `0x2a35a324` | BuybackTreasury, BuybackTreasuryV2 |\n| `TooFewDollars(uint256)` | `0xa9062c20` | TickerLauncher |\n| `TooSoon()` | `0x6fed7d85` | BuybackTreasury, BuybackTreasuryV2 |\n| `UnknownLaunchConfig()` | `0x67905872` | Factory |\n| `UnknownMarket(address)` | `0x99b75a5c` | MarketTickerLauncher |\n| `UnknownPool()` | `0xf7139e33` | ManagedTickerHook |\n| `UnknownProvenance(address)` | `0xcb221e63` | QuoteRegistry |\n| `UnknownToken()` | `0x8698bf37` | Factory, ZapRouter, MarketZapRouter |\n| `UnsupportedKind(address,uint8)` | `0x38325e2f` | BuybackTreasuryV2 |\n| `WalletCapExceeded(address,uint256,uint256)` | `0x683b3827` | Token |\n| `WrongContext()` | `0xf9d1dd2e` | ManagedTickerToken |\n| `ZeroAddress()` | `0xd92e233d` | Factory, QuoteRegistry |\n<!-- selectors:end -->\n\n## A minimal indexer\n\nFive log streams rebuild everything the site shows, with no tickr service in the loop:\n\n1. **Launches.** `TokenLaunched` on the factory, from its start block in [07 addresses](./07-addresses.md). It names the coin, its pool id, the deployer and the pair. A coin launched through a launcher fires it too; the launcher's own `Launched` and `FirstBuy` land in the same transaction and say which ticker it sits under and what the opening buy took.\n2. **Trades.** `Swap` on the pool manager, filtered by pool id. Amounts are from the pool's side: the currency it took in is negative and includes the fee, the one it paid out is positive, so a negative delta on the coin's side is a sell and a positive one a buy. Only the pool manager holds reserves, so there is no internal swap to skip. A `SnipeTaxed` on the coin in the same transaction says how much of a buy in the first five seconds went to the dead address.\n3. **Fees.** `FeesCollected` on the locker, one per collection, with the split already applied: the protocol's, the creator's and the club's parts of the quote side, and what the coin side credited and burned. The sum of `burnedCoin` is the dead address balance.\n4. **The club.** `ClubFee`, `ClubClaimed` and `DeadPotSwept` on the ticker launcher, keyed by ticker and epoch.\n5. **The buyback.** `BoughtAndBurned` on the treasury, one per buy of TICKR.\n\nTwo rules of thumb. Backfill in bounded block ranges: the public RPC refuses a wide `eth_getLogs` window. And index a launch before the trades of the same block: the opening buy lands in the launch transaction.\n\n## Holder counts\n\nA coin's `Transfer` log is its holder ledger. Leave these addresses out when counting holders; they hold coins for the system, not for themselves:\n\n- the pool manager: the pool's reserves;\n- the launch locker and the fee escrow: fees in transit and creator credits waiting to be claimed;\n- `0x000000000000000000000000000000000000dEaD`: burned;\n- the launch seeder, the zap router, the launch-and-buy router and the ticker launcher: pass-through only, holding nothing between transactions.\n\n## Errors\n\n| error | where | what to do |\n| --- | --- | --- |\n| `LaunchEconomicsMismatch` | factory | the terms changed since your preview. re-read the preview and retry. |\n| `LaunchFeeNotPaid` | factory | value must equal `launchFee`. |\n| `BadValue` | ticker launcher, seeder | value must equal `launchFee + NEW_TICKER_FEE` for a new ticker, `launchFee` for an existing one. |\n| `CreatorTaxTooHigh` | factory | the tax is above `maxCreatorTaxBps`. |\n| `TickerReserved`, `NameReserved` | factory, ticker launcher | the symbol or name belongs to an official asset. |\n| `PairTokenNotApproved`, `PairTokenDecimalsMismatch` | factory | the quote asset is not on the list, or its decimals changed. |\n| `LaunchConfigDisabled`, `UnknownLaunchConfig` | factory | the config id is closed or does not exist. |\n| `NotWhitelisted` | factory | launches are gated. check `canLaunch(address)`. |\n| `NotRegistrar` | registry | only a registrar may add anchors. |\n| `Slippage` | seeder, zap | the output fell under `minOut`. |\n| `BadPath`, `Expired` | zap | the route does not end at the coin's pool, or the deadline passed. |\n| `EpochOpen`, `NotUnderTicker`, `NothingToSweep` | ticker launcher | the club epoch is still running, the coin is not under that ticker, or the pot is empty. |\n| `QuoteIsAnchor`, `QuoteLaunchedHere`, `NoMarket`, `QuotePriceUnavailable`, `NoTargetRaise` | market quote launcher | the token is an anchor or a tickr coin (use that path), has no v3 pool over the floor, or its price cannot be read. |\n| `PoolAlreadyExists` | seeder | somebody opened the coin's pool key first. change the salt and launch again. |\n| `SnipeTaxed(to, amount, bps)` | the coin | a buy inside the first five seconds paid its snipe tax to the dead address. an event, not an error. |\n| `LaunchBlock` | the coin | a buy out of the pool in the launch block by a wallet that is not the launch's own. buying opens next block. |\n| `WalletCapExceeded(recipient, held, bought)` | the coin | inside the two blocks after the launch block, the buy would leave the wallet above 5% of supply held or 5.5% bought. nothing partial. |\n| `PositionNotLocked`, `NotPositionManager` | seeder, locker | the position did not land in the locker, or something other than the position manager tried to hand it one. |\n| `RefundFailed` | seeder | the input the pool did not take could not be sent back to the caller. |\n| `EmptyMetadata`, `MetadataTooLong`, `BadName`, `BadSymbol` | deployer | name or symbol empty, a field over its limit, a name with a space at either end, two spaces in a row or a byte outside printable ASCII, or a symbol with anything but letters and digits. |\n| `QuoteDecimals` | market quote launcher | the token has fewer than six decimals. |\n| `SymbolTooLong`, `BadSymbol` | ticker launcher | an invented name is over twelve bytes, or has a character that is not a letter or a digit. |\n| `NotAMint` | locker | somebody tried to transfer an existing position into the locker; only launch mints land there. |\n| `OnlyTickerLauncher` | seeder | only the ticker launcher named on the factory may open a dollar pool. |\n| `OnlyExecutor`, `NotOneDollar`, `OutsideTheBand`, `PriceGuard` | guard hook | the dollar pool refuses anything but the seeder's own opening, a position off the band, or a swap that would move the price. |\n| `NotTheFrozenFee(baseFeeBps, creatorTaxBps)` | market ticker launcher | the launch config handed in does not carry the frozen 82 bps base fee, or the creator tax is not zero. the launcher refuses rather than opening a coin at a fee the factory happened to have configured. |\n| `MarketExhausted(tokenIn, tokenOut)` | market zap router | the name's market has nothing available in the direction the trade needs. the router checks the pool's price before it swaps and stops, rather than filling at whatever is left. on the site: *This route currently has no available liquidity in the required direction.* |\n| `PriceLimitAlreadyExceeded(a, b)` | Uniswap v4 | selector `0x7c9c6e8f`, raised by the pool itself when the price is already past the bound the swap carries. reaches the site through `WrappedError` and reads the same as `MarketExhausted`, because for a trader it is the same condition. |\n| `TermsWeakerThanPolicy(token, offered, floor)` | treasury v2 | a conversion was offered on terms below the floor in force for that name. |\n| `NothingEarmarked`, `NothingToBuy`, `TooSoon`, `NotYetLaunched` | either treasury | there is nothing set aside, the tranche sized to zero, the ten minute interval has not passed, or the official coin does not exist yet. |\n\n## Versioning and attribution\n\nThe contracts are immutable; a new version is a new factory and a new deployment record. If you build on tickr, say so and link the site; do not present your service as run by tickr.\n",
  "19-market-quotes.md": "# 19 · Any token with a market (Mode 5)\n\n> Not in use. `MarketQuoteLauncher` is deployed at `0x4Dc4404ac80fC4372B689f0013b8adACf1E0B61b` but is not\n> wired into the site, so this mode is not offered on the create page. The page describes what the contract does.\n> For launches priced in a name somebody invented, see [05 anchors](./05-anchors.md).\n\n> One of the quote assets a launch can use. The others are ETH, USDG and Stock Tokens (04), an invented name (05), and a tickr coin (10).\n\nA launch can be priced in **any ERC-20 on Robinhood Chain with a deep enough Uniswap v3 pool**: a pool against WETH or USDG, from the canonical v3 factory, holding at least the floor of that counter asset. `MarketQuoteLauncher` reads the pool at the moment of launch, sizes the opening market cap from it, and opens the coin's own pool against the token like every other launch.\n\n## What counts as a market\n\n| rule | value |\n| --- | --- |\n| pool | Uniswap v3, canonical factory, any fee tier |\n| counter | WETH first; USDG only when no WETH pool clears both floors |\n| floor, total | 5 WETH, or 15,000 USDG, held by the pool (`minDepth`) |\n| floor, at the price | the same amount the pool would pay out to sellers of the token within 5% of the current price (`BAND_BPS`), measured by walking the pool's ticks |\n| decimals | six or more |\n| price | the pool's spot price when the launch is sent |\n\nThe second floor is the one that matters: it is the counter asset a seller could actually take out near the price, which cannot be faked with free liquidity of the token itself, and capital parked far out of range does not count. It is still a reading of the current block: a flash loan can satisfy it for one transaction. For that reason this mode ships switched off (`ENABLE_MARKET_QUOTES`), and the site hides it, until the check also asks the pool's own price history. Among the pools that clear both, the one that absorbs the most within the band wins. A WETH market always beats a USDG one, so a token with both is priced against ETH.\n\nThree kinds of token are refused here because a better price source exists: ETH, USDG and Stock Tokens go through their own paths (04, 11), and coins launched by this factory go through the coin quote launcher (10), which reads their own v4 pool.\n\n## The opening market cap\n\nThe launcher converts the ETH target (or the USDG target for a USDG market) into the token at the pool's spot price, and takes forty percent of that as the phantom reserve, the same ratio every other mode uses:\n\n```\nthreshold  = targetRaise[base] * 1e18 / basePerQuoteX18\nphantom    = threshold * 4000 / 10000\n```\n\n`basePerQuoteX18` is the pool's `sqrtPriceX96` squared, oriented so it reads as WETH (or USDG) raw units per 1e18 raw units of the token. The result is denominated in the token's own decimals. In dollar terms the opening market cap matches an ETH or USDG launch on the day, because it is the same target converted.\n\n## Spot, and the pin\n\nThe price is spot, not an average. The floor at the price is a cost, not a lock: somebody who keeps a deep pool at a false price for the whole time between your preview and your launch can still show you that price, and the site shows the market cap in dollars for exactly that reason. Look at it. What protects the creator is `expectedEconomics`: the preview hashes the terms, the launch recomputes them from the pool and reverts on any difference. A price that moves between preview and send fails closed. Somebody who moves the pool in the same block can only make a launch revert, never change its terms.\n\n## Reads\n\n```solidity\nfunction bestMarket(address quote) view returns (Market memory);      // pool, counter, fee, depth; reverts with the reason\nfunction quotePrice(address quote) view returns (Market memory, address base, uint256 basePerQuoteX18);\nfunction quoteEconomics(address quote) view returns (PairEconomics memory);\nfunction previewLaunch(uint256 launchConfigId, address quote) view returns (bytes32 expectedEconomics, PairEconomics memory, Market memory, address base, uint256 basePerQuoteX18);\nfunction isEligibleQuote(address quote) view returns (bool);           // never reverts\nfunction minDepth(address counter) view returns (uint256);\nfunction targetRaise(address base) view returns (uint256);\n```\n\n## Launching\n\n```solidity\nfunction launchWithMarketQuote(TokenParams params, uint256 launchConfigId, address quote) payable returns (address token, bytes32 poolId);\nfunction launchWithMarketQuoteAndBuy(TokenParams params, uint256 launchConfigId, address quote, uint256 quoteIn, uint256 minTokensOut) payable returns (address token, bytes32 poolId, uint256 tokensOut);\n```\n\n`value` is the launch fee. The first buy is paid in the token itself, pulled from the caller and spent in the new pool in the same transaction. Errors: `QuoteIsAnchor`, `QuoteLaunchedHere`, `NoMarket`, `QuotePriceUnavailable`, `NoTargetRaise`, `BadValue`.\n\n## Buying such a coin with ETH\n\nThe zap ([12](./12-zap.md)) walks ETH into the token through its v3 pool, or through ETH/USDG and then a USDG pool, and then into the coin's own pool, in one transaction. Selling walks the same route back.\n\n## Owner powers\n\nThe owner can change `minDepth` per counter and `targetRaise` per base, for future launches only. Every launch already made keeps the terms frozen at its launch.\n\n## The list on the create page\n\nNot shown, because the mode is off. The site's address record carries `marketQuoteLauncher` as the zero address,\nand every code path that would read the launcher checks for that first and falls back, so the create page does not\noffer this choice and no list is built.\n\nIf it is ever switched on, the list is read from the chain with the same rule the contract applies, so what is\nshown is what the launcher accepts, and the preview before signing is the contract's own.\n",
  "20-reference-alignment.md": "# 20 · Reference versus production\n\nThe managed ticker system and the two activation buys were built from a reference that traded on the public chain on 2026-09-07 (PING priced in PONG, two activations, then a buy and a sell through an aggregator's router). This page lists every difference between that reference and what tickr deploys, why each exists, and what validates it. A difference is not a vulnerability, but nothing a difference touches inherits the reference's evidence; each is tested on its own. On the site the activation is called the listing: \"listing pending\" until the two buys land, \"list your coin\" for the creator.\n\n## Preserved as the reference had it\n\n| Piece | Reference | Production | Validation |\n|---|---|---|---|\n| The wrapper | `ManagedTickerToken.sol` | the same file, one line changed: the hook binding is read from the shared hook's registry (`tokenOf(poolId)`) instead of a per-wrapper `token()` | `test/ManagedTickerMarket.t.sol` is the reference's suite ported unchanged; `ManagedTicker.t.sol` and `Ticker.t.sol` on the production path |\n| The hook's permissions | the five plain callbacks, no swap deltas, no dynamic fee | identical | `test_hook_ordinaryAccountingOnly_andBoundToTheLauncher` checks the flags on the deployed address |\n| Pool terms | fee 500, tick spacing 1, positions [-2, 0], [0, 2] and the retained [-2, 2] | identical | the market suite |\n| Inventory floor, donation, cash keep, retained sides, recentre budget | 10,000e6, 1 USDG minimum, 0.01 USDG, 0.001 units, 4,096 raw | identical | the market suite; `INVENTORY_FLOOR` is a launcher constant |\n| Activation route | native ETH/USDG, then the name's pool, then the coin's pool, through the canonical Universal Router, output to the wallet, `0x1004` / `0x070c0f` | identical bytes: `UniversalRouterBuy.sol` (scripts, fork test) and `web/src/lib/activation.ts` (site) encode the same `ExactInputParams` struct | `test/UniversalRouterBuy.t.sol` compares the script's full `execute` calldata to golden bytes produced by the site's encoder; `test_fork_activation_throughTheCanonicalUniversalRouter` on the live chain's router and quoter; the offline cases in `web/scripts/tests/activation.test.cjs` |\n| Activation amounts and tolerance | 0.0005 ETH, then 0.001 ETH; one percent under a fresh quote | identical defaults (`GENESIS_QUOTE_BUY_ETH`, `GENESIS_COIN_BUY_ETH`; the site's `AMOUNTS`) | any other amount needs its own acceptance test, as the reference said |\n| Both stages, always | two purchases also under an existing name | identical: the site never skips the name purchase | `useActivation` has no skip path |\n| The wallet journal | intent saved before the wallet opens, hash saved on return, unknown outcomes reconciled by hand, one signing lock per wallet across tabs, receipts verified for canonical inclusion, order, swaps on the exact pools and delivery | ported, scoped by chain, wallet and coin | the offline cases; the R15 closure checks in the brief |\n\n## Different, and why\n\n| Piece | Reference | Production | Why | Validation |\n|---|---|---|---|---|\n| One hook per wrapper | a hook deployed per wrapper with the wrapper's address in it | one `ManagedTickerHook` for every name, with a registry the launcher fills at creation | a creator cannot mine and deploy a hook address inside a launch; one shared hook keeps a launch to one transaction | `test_hook_aSecondNameGetsItsOwnPoolBehindTheSameHook`, the registration and binding tests, the fork test on the canonical pool manager |\n| Who creates the wrapper | a pilot adapter that installed one predeployed wrapper | `TickerLauncher._create` through `ManagedTickerDeployer` (CREATE2, the creation code kept out of the launcher for size) | the general issuer path the reference asked for | `predictTicker` equals the created address; the ordering tests; the size report |\n| Name addresses | not constrained | every name starts with `0xF`; every coin must sort below its name (`CoinNotFirst`) | the reference's orientation (coin first), enforced for every launch rather than checked by hand | `test_ticker_predictionMatchesCreation_andSitsInTheTopSixteenth`, `test_ticker_aCoinThatWouldSortAboveItsNameIsRefused`, the genesis and seed grinds |\n| The name fee | a 1 USDG donation from the deployer | the 0.0015 ETH ticker fee converted through the live ETH/USDG pool with a floor of 1 USDG, all of it the wrapper's surplus | the launch pays for its own pool; no separate funding step | `test_ticker_inventingANameOpensItsOwnDollarPool`, the fork test's real USDG surplus |\n| Genesis | not part of the reference | FUN and TICKR go through the same launch path and the same two activation buys from the deployer; without the router and the quoter in the record genesis refuses to send anything, and the only exemption is explicit (`GENESIS_ALLOW_NO_ACTIVATION`), off the public chain, and written into the record as `genesisActivated: false` | the official coin follows the rule it sets | `Genesis.t.sol`, the fork cycle |\n| Site routing for ordinary buys | not part of the reference | the zap wraps one for one or trades the name's pool, whichever pays more; a buy above the pool's offer wraps | a creator's buyers should not hit the pool's per-swap cap | `Zap.t.sol`, `test_managed_ethIntoACoinThroughTheNamePool_oneUnlock` |\n\n## Not proven by the reference, said plainly\n\n- The reference proves one sequence at one size on one day. It does not prove instant listing, listing at every size, dollar prices on every chart, or that any aggregator will route a given trade.\n- A name's pool takes one visit per transaction. Split routes that cross it twice are unsupported and revert.\n- The offer through a name's pool is finite: the floor plus four times the circulation, per swap. Larger buys wrap.\n- A confirmed activation is \"on-chain sequence confirmed, external trading unverified\" until a real buy and sell through an outside router are recorded for that launch; the launch runbook records them.\n- The site's first buy in a launch is paid in USDG the creator already holds (an approval, then the launch); the reference funded its setup differently. The order of the activation is the reference's; the count of signatures per mode is not promised.\n- The fork test measures what the route put into the coin's pool from the route's own swap log, not from the wrapper's books, and then checks the wrapper's backing against that.\n\n## After the R16 review\n\n- **Unknown submissions** (R16-M01). An attempt whose hash the wallet never returned stays unresolved until the person pastes the hash the wallet shows, which is then matched against the reviewed request. Nothing about the chain's view of the wallet's nonce turns unknown into \"never sent\". The one definite decline is the wallet's own pre-broadcast rejection. A browser without a lock manager signs nothing. The workflow lives in `web/src/lib/activationFlow.ts` with every dependency injected, and `web/scripts/tests/activationFlow.test.cjs` drives it through a lost wallet answer, a transaction that appears late, a reload, a second tab, no lock manager, a decline, a pending receipt, a reverted purchase, an expired review, a moved nonce, a smart account, a zero or failing quote and a storage that does not keep writes.\n- **Creation recovery** (R16-M02). A launch has a journal of its own (`web/src/lib/launchJournal.ts`): the seed, the predicted address and what the launch is are written before the wallet opens, the hash the moment the wallet returns it, the coin once its receipt is read. The receipt the runner waited for is reused, never fetched twice; a read that fails after the broadcast leaves the record with its hash and the page settles it, reads only, on the next visit; a wallet answer that was lost blocks new launches until the hash is matched or the person confirms the wallet shows nothing; a resumed launch keeps its seed, so a second signature could only be the same address, which cannot deploy twice. The transaction link stays visible on an error.\n- **Completion for every mode** (R16-M03). Only a launch under an invented name enters the activation step; every other launch shows its receipt, the coin's address and the link to it.\n- **Genesis** (R16-M04). Without the router and the quoter in the record, genesis refuses before it broadcasts anything. The only exemption is `GENESIS_ALLOW_NO_ACTIVATION`, refused on chain 4663, and it writes `genesisActivated: false` into the record.\n- **Addresses** (R16-L01). The record is written in checksum form by the sync step and read in checksum form by the site; the example record is checksummed; the activation suites pass on the shipped tree as it is.\n- **Labels and signals** (R16-L02). A stage is called confirmed only by a verified receipt; without an inspection it is unknown. The public signal treats an empty-code answer of `\"0x\"` or `undefined` as an ordinary wallet and a thrown read as unknown, finds the launch transaction from the pool's `Initialize` log rather than assuming the first swap, and the grid's mark is that same signal, asked only for fresh coins under a name.\n- **Docs** (R16-L03). The wrapper's description in 05 is the managed one throughout: inventory against circulation, backing at or above circulation, conversion one for one while the pool manager is locked, and no claim of a guaranteed dollar or of routing elsewhere.\n- **Calldata parity**. `UniversalRouterBuy.sol` encodes the same `ExactInputParams` struct the site encodes; `contracts/test/UniversalRouterBuy.t.sol` holds golden bytes produced by the site's encoder and fails on any byte that moves.\n\n## After the R17 review\n\n- **Creation recovery** (R17-M01). Creation now runs on its own engine (`web/src/lib/launchFlow.ts`) with the same discipline as the activation: the wallet's signing lock is held across tabs for the whole launch (shared with the activation, so a wallet signs one thing at a time on the site); the record is read and validated under that lock before anything is prepared; the intent carries the exact target, calldata, value, nonce, chain, wallet and predicted coin; the nonce is fixed right before the wallet opens; the hash is written the moment the wallet returns it, by the launch step alone, so an approval's hash can never be taken for the launch; an unresolved launch has no dismissal, it is settled by its hash, matched against the reviewed transaction (sender, target, calldata, value, nonce, chain), or by the wallet's own pre-broadcast rejection; a receipt settles a launch only when the factory itself emitted the launch of the predicted coin; a record this browser cannot read is unresolved, not absent. The transaction runner resets its per-run refs before any guard. Seven offline lifecycle cases in `web/scripts/tests/launchFlow.test.cjs`: intent before the wallet, a lost answer, old or mistaken hashes, another coin in the receipt, a reload, a second tab, no lock manager, damaged and unreadable storage, a write that does not land, an approval followed by a guard failure, a reverted launch.\n- **The live schema** (R17-M02). The record's schema lives in one module (`web/scripts/lib/recordSchema.mjs`), used by the release guard and tested with a record shaped exactly as genesis writes it. `genesisActivated` is a field of the schema and a live build requires it to be `true`; a rehearsal record or an unactivated genesis is refused with its reason.\n- **Displays** (R17-L01). Everything the activation hook reads or reviews is stamped with the wallet, chain and coin it was read for and is never shown for another identity, so a read that fails after a wallet change cannot leave a stale done. The public signal returns unknown when the scan is truncated (more swaps than it inspects and none of the inspected qualify), and the coin branch accepts a delivery only into an ordinary wallet, like the name branch.\n- **Tests** (R17-L02). Every offline suite starts from one fixture (`web/scripts/tests/fixture.cjs`) and passes on its own against the checked-in zero record. The golden calldata lives in `golden.json`, is checked by the activation suite against the site's encoder and against the hex in the Solidity test, and `node scripts/tests/golden.cjs --write` regenerates both after a reviewed change.\n\n## After the R18 review\n\n- **Delayed settlement** (R18-M01). Every transition of the creation engine, the reads-only ones included (`settle`, `recover`, `clear`), runs under the wallet's signing lock across tabs, and every write re-reads the record and refuses when it is no longer the one the step started from, so a delayed result for an older launch can never overwrite a newer one. The stored record is validated in full by status: a sent or launched record without its hash, a launched record without its coin and block, an unknown status or a prepared record carrying a hash are refused as unresolved. The creation receipt is revalidated against its canonical block hash and waits for one more block, as the activation does.\n- **Repricing** (R18-L01). The runner reports a repricing with the replacement's identity; the engine adopts it only when sender, target, calldata, value, chain and nonce are the reviewed ones, keeping the first hash alongside; a repricing into another request stops the runner for review; a repriced launch can also be matched by hand. `web/src/lib/txSteps.ts` holds the runner's settlement so the test drives the runner and the engine together.\n- **Golden fixture** (R18-L02). The offline suites replace the checked-in deployment record with the example's zeros inside their own process, so the fixture's addresses are what every module sees on any machine; the golden bytes and the Solidity test's constants are regenerated from that fixture and checked by the activation suite.\n- **Approvals** are signed inside the same lock as the launch; the documentation's claim that a wallet signs one thing at a time on the site now holds for the whole launch. An older record from a previous build (`tickr.launch.v1`) blocks a new launch until it is settled by hand. The runbook's checklist carries the acceptance gate the review asked for: a real buy and sell of the official coin through GMGN's router after its activations, plus a throwaway name and a second coin under it through the site, before the public switch.\n\n## Upload protection, 2026-09-07\n\nThe image upload went through three states in one day. It began as a strict gate (twelve an hour and three a minute per address, and a build that could not reach its counter refused everything), which a hand test on the preview hit with nothing uploaded. It was then opened completely. An outside reviewer pointed out the same day that an origin header is not authentication and that an open route lets a script spend the pinning plan.\n\nWhat stands now, in order, all of it invisible to a creator:\n\n1. The origin check, advisory as before.\n2. A signature from the creator's wallet: one free `personal_sign` per wallet per hour, kept in the tab, naming the wallet, the chain and the expiry. The server verifies it and asks the chain that the wallet holds at least 0.0001 ETH, so a script has to fund every address it uploads from. The preview, which replays a recording and has no wallet, skips this step and keeps everything else.\n3. Shared counters in the tokens Worker, one Durable Object per name: thirty uploads an hour per address, thirty per wallet, two thousand an hour for everyone, and ten thousand a day as the hard spending budget. A site that cannot reach them counts per instance instead.\n4. A memory of pinned files by content hash, so the same bytes are never sent to Pinata or counted twice.\n5. One JSON log line per decision in the Worker's observability, and `GET /api/pin` shows the live counters.\n\nWhenever the route says no, for any reason other than the file itself, the create page stores the image with the coin on-chain, the same path it takes where pinning is not set up, so a creator is never stopped and never sees a refusal. The old burst limiter was also found never to count in production (twenty calls in a row against a limit of ten all passed) and is gone.\n\n## After the R19 verification, 2026-09-07\n\nThe verification of build 19 confirmed the three R18 repairs and left two creation-flow items, two disclosure items and the qualifications from the external review of the same day. Every item, and what was done:\n\n- **R19-M01, creation could sign from a different wallet than the one reviewed.** Fixed. The runner takes the reviewed identity (`RunOptions` in `web/src/hooks/useTx.ts`): right before each wallet request it asks the connector for its live account and network and stops, with nothing signed, when either differs; and it binds `account` and `chainId` into the request itself. Creation passes the reviewed wallet and chain for both the approval and the launch.\n- **R19-L01, a failure before the launch was asked of the wallet stranded the journal.** Fixed. The decision after a run is one pure function, `afterRun` in `web/src/lib/launchOutcome.ts`, with its own offline suite: a run that never invoked the wallet's send is \"not sent\" and clears the prepared record; a post-approval simulation failure does the same; only a run that reached the wallet keeps an unknown outcome. The runner's own pre-send checks (the factory-code check, the identity check) fall on the \"not sent\" side.\n- **R19-L02, fee and buyback text.** Fixed: the struct comments in `06-fees.md`, and the two places in `SPEC.md` that still described the buyback split as 80/20. The shares are 50/10/40 under a name, 60/0/40 elsewhere; the treasury starts half and half and the burn share can only rise.\n- **R19-L03, preview addresses.** Fixed: the preview build says whose addresses they are and shows them without explorer links.\n- **The optional first buy's minimum.** Tightened: a first buy whose preview rounds to nothing is refused before anything is prepared, and the request is never built with a zero minimum. The preview is a pure function of the amount and the economics pinned into the transaction, so there is no clock to expire; a change in economics reverts the launch instead.\n- **Lint, headers, dependencies, CI.** `eslint .` is clean (the build output is ignored, the CommonJS suites are allowed to `require`). Security headers ship from `next.config.ts`. Overrides live in `pnpm-workspace.yaml`, where pnpm 10 reads them, with `uuid` pinned; `pnpm audit` is clean. A workflow runs the contracts (build with sizes, unit, fuzz at a thousand runs, invariants), the site (audit, lint, suites, golden bytes, build, typecheck) and the tokens Worker on every push.\n- **Owner, epoch, anti-snipe, capacity, Mode 5, legacy records.** Accepted as designed and disclosed: the owner's three-day fee-recipient reassignment stays (the takeover path) and the owner is a hardware wallet, not a multisig, by the owner's decision, disclosed in `09-risks.md`; pots land in the window of the sweep (`05-anchors.md`); launch protection is a best-effort deterrent for standard routers (`02-lifecycle.md`, the token page); one wrapper-pool visit per transaction; Mode 5 stays off; legacy records are quarantined, not migrated.\n- **The acceptance gate** stays a launch-day step in `LAUNCH.md`: real buys and sells through GMGN on the exact release, with hashes, before the public switch.\n\n\n## The market release\n\n- **Fixed-inventory names.** A second kind of name, issued by `MarketTickerDeployer` through `MarketTickerLauncher`: 500,000,000 at 6 decimals in its own USDG pool, no mint after birth, no redeem, no backing. Redeemable names are unchanged and every name made before this release stays one. `QuoteRegistry` tells them apart by provenance.\n- **The frozen fee.** `MarketTickerLauncher` reverts with `NotTheFrozenFee` unless the config it is handed carries 82 bps and a zero creator tax. The factory gates only on registrar and accepts whatever config id a caller passes, so the check had to live in the launcher.\n- **The allocation.** Only `protocolFeeRecipient` changed, to `BuybackTreasuryV2`; every other field of the policy was carried through unchanged. A fixed-inventory name is not in the ticker club, so the club's 1000 bps folds into the creator's and the split freezes at 60 / 0 / 40. Earlier drafts of this release described it as 40 / 30 / 30; that was wrong and is corrected here and in [06 fees](./06-fees.md).\n- **The second treasury.** The first treasury has no way to value a name that is not a dollar and forwards what it cannot convert to the team whole, so the burn share of fixed-inventory name revenue would have been zero. `BuybackTreasuryV2` converts through the name's own market instead. It allocates nothing differently for anything the first treasury could already convert.\n- **Exhaustion reads as a sentence.** `MarketExhausted` and Uniswap's `PriceLimitAlreadyExceeded` (`0x7c9c6e8f`) both surface as *This route currently has no available liquidity in the required direction.* No promise is made that selling back or a smaller trade will work; either needs its own fresh quote.\n- **The keeper is switched off.** It signed before broadcasting, recorded the hash and nonce first, never resent an unresolved send, took a durable lease with an ownership token, refused a padded estimate over the ceiling, and alerted after two runs that could do no work. It then ran one verified live cycle and was stopped at the owner's instruction. Nothing collects on a schedule now; `collectFees` is permissionless and always was.\n- **Not done, and disclosed.** The site does not read `QuoteRegistry`, so it cannot price a fixed-inventory name or a coin quoted in one; those show no dollar value. `MarketQuoteLauncher` is deployed but not wired, so mode 5 is not offered. Chart sites still cannot price a pair quoted in a name they do not know.\n",
  "README.md": "# tickr protocol docs (v1)\n\n\n> Independent, immutable contracts, independently deployed. tickr is not affiliated with Robinhood.\n\nSource: [github.com/tickrdotfun/tickr](https://github.com/tickrdotfun/tickr). The design source of truth is [`SPEC.md`](./SPEC.md). The pages below are written from the Solidity in `contracts/src/` and only describe functions, events, errors, and structs that exist there.\n\n## Pages\n\n| Page | What it covers |\n|---|---|\n| [01 Overview](./01-first-principles.md) | no custody by the operator, atomic flows, everything readable on chain, immutable per version |\n| [02 What a launch does](./02-lifecycle.md) | create, trade, collect: one transaction opens the coin and its locked pool; the five-second snipe tax |\n| [03 Price and market cap](./03-curve-math.md) | the one-sided position as a constant product curve, opening market caps, fees in the math |\n| [04 ETH, USDG and Stock Tokens](./04-custom-pairs.md) | mode 1. the three official quote assets, per-asset economics, the `expectedEconomics` pin, native vs ERC-20 calling conventions |\n| [20 Reference versus production](./20-reference-alignment.md) | what the managed ticker system keeps from the reference that traded, what differs and why, and what each difference is validated by |\n| [05 Invented names](./05-anchors.md) | the two kinds of name: a redeemable one-for-one wrapper of USDG, and a fixed-inventory name worth what its market says. inventing one, launching under one, why nobody owns it, the name's own pool, the club, the required disclosure |\n| [06 Fees](./06-fees.md) | launch fee, the pool fee, creator tax, the 50/10/40 split, collecting, burning, claims, where the protocol share goes |\n| [07 Addresses](./07-addresses.md) | chain facts, canonical Uniswap v4 and v3 addresses on Robinhood Chain, the contract table |\n| [09 Risks](./09-risks.md) | what can go wrong, the exact list of owner powers, immutability |\n| [10 tickr coins](./10-coin-quotes.md) | mode 3. pricing a launch in a coin this factory launched, and the guardrails on it |\n| [11 Stock Token quotes](./11-stock-quotes.md) | mode 4. pricing a launch in a Stock Token, sized from its live Chainlink feed |\n| [12 Buying with ETH](./12-zap.md) | the ZapRouter: any coin, one transaction, paid in ETH |\n| [13 Official coin](./13-official-coin.md) | TICKR priced in FUN: the first launch, on the same rules as every launch, how it was made first, and the buyback that burns it |\n| [14 Liquidity lock](./14-liquidity-lock.md) | what the locker holds, what it can do, what it cannot, and how to check |\n| [15 Launching from code](./15-launching-from-code.md) | the pinned preview, every quote asset, knowing the address first, metadata |\n| [16 Trading from code](./16-trading-from-code.md) | one pool, routing from ETH, collecting fees and claiming |\n| [17 Reading state](./17-reading-state.md) | the launch record, pool key and price via `extsload`, fees owed, ticker views, the treasury's views, the site's endpoints |\n| [18 Events and errors](./18-events-and-errors.md) | the full event list, every named error and what to do about it |\n| [19 Any token with a market](./19-market-quotes.md) | mode 5. pricing a launch in any token on the chain with a deep enough Uniswap v3 pool against WETH or USDG |\n\n## Conventions used in these docs\n\n- \"Quote\" is the asset a curve is priced in: native ETH (`address(0)`) or an approved ERC-20. \"Token\" / \"meme\" is the launched ERC-20.\n- All bps values are out of `10_000`.\n- Solidity signatures are quoted as they appear in `contracts/src/`. Every address in [07 addresses](./07-addresses.md) is the live one on chain 4663 and matches `contracts/deployments/4663.json`, which is what the site is built from.\n",
  "SPEC.md": "# tickr, Protocol Specification (v2)\n\nThe build spec, in the order the protocol runs. Everything here is checkable against `contracts/src`.\n\n## 1. First principles\n\nNo custody by the operator, atomic launches, everything readable on chain, immutable per version, the creator holds nothing at creation except a disclosed dev buy, every coin ends in 6942. See [01](./01-first-principles.md).\n\n## 2. Lifecycle\n\nOne state: live. `Factory._launch` deploys the coin (`LaunchDeployer.deployToken`, CREATE2, salt namespaced by the initiator), opens its Uniswap v4 pool at the opening price with no hook, tick spacing 10, fee `(baseFeeBps + creatorTaxBps) * 100`, mints one position holding the entire supply from the opening tick to the end of the range to `LaunchLocker` (`LaunchSeeder.seedLaunch`), records `LaunchedToken` and copies the `FeePolicy` in force. A dev buy is a swap on the new pool in the same transaction through `LaunchAndBuyRouter` or a launcher. No graduation, no phases, no rescue. One snipe tax, in the coin itself: a transfer out of the pool manager in the coin's first five seconds pays 9900 / 2500 / 300 / 50 / 10 bps by elapsed second, burned to `0xdEaD`; the launch's deployer, its fee recipient and the locker are exempt; sells never pay. `Token.currentSnipeTaxBps(recipient)` is the rate a quote must apply. Launch protection beside it, in blocks: in the launch block only exempt wallets may buy (`LaunchBlock`); in the two blocks after, every other wallet is held to 5% of supply held, by buys and by transfers in, and 5.5% bought, net of the tax (`WalletCapExceeded`); the exempt set is the launch's deployer and fee wallet, the factory, the seeder, the pool manager, the locker, the fee escrow, the protocol's fee wallet and the dead address; both rules see only ERC-20 transfers out of the pool manager, so ERC-6909 claims are outside them, by design and disclosed; `protectionEndsAtBlock`, `remainingBuy`, `remainingHold` say where a wallet stands.\n\n## 3. Pool math\n\nOpening price `p0 = phantomQuote / supply`. A one-sided position from `p0` upward is a constant product pool with a virtual quote reserve `P = phantomQuote`: coins left `t = P S / (P + q)`, market cap `(P + q)^2 / P`. Fees are the pool's LP fee, taken from the input of every swap. See [03](./03-curve-math.md).\n\n## 4. Pairs\n\nETH (config economics), USDG and any ERC-20 the owner approves with `setPairTokenEconomics(pair, phantomQuote)` and the registry lists; registrars supply economics for the rest: `TickerLauncher` (redeemable names, USDG economics), `StockQuoteLauncher` (official Stock Tokens, opening cap from the Chainlink feed and a USD target), `CoinQuoteLauncher` (coins launched here, opening cap from the quote's own pool), `MarketQuoteLauncher` (any token with a deep enough Uniswap v3 pool against WETH or USDG, opening cap from that pool's spot price, floors 5 WETH or 15,000 USDG). Every launcher pins economics with `expectedEconomics`.\n\n## 5. Redeemable names\n\n`ManagedTickerToken`: a one for one wrapper of USDG, mint and redeem while the pool manager is locked, no owner, that runs its own WRAPPER/USDG pool (fee 500, spacing 1) behind the one `ManagedTickerHook`. Its unsold inventory is the buy side (a floor of ten thousand dollars' worth plus four times the circulation), its backing the sell side; before every outside swap it rebuilds both and re-centres at one dollar from its own surplus, after it checks a whole fill, the price inside two ticks and backing at or above circulation; one visit per transaction. `TickerLauncher.launch(symbol, coin, cfg)` creates the wrapper at CREATE2 by symbol if new, at an address in the top sixteenth of the space, charging `NEW_TICKER_FEE` (0.0015 ETH) on top of the launch fee, which becomes the wrapper's working surplus through the live ETH/USDG pool; the coin must sort below the name (`CoinNotFirst`), so it is always currency0. The club: 10% of the base fee of every coin under a ticker goes to `pot(ticker, epoch, coin)` on collection; `recordVolume` credits the same epoch; `claimClub` pays a member `pot * w / W` per payer, by amount, where `w` is the member's volume, doubled when it is the club's captain, and `W` is the ticker's volume plus the captain's volume; `captainOf(ticker, epoch)` is the founder's coin (the first launched under the ticker) while it has volume in the epoch, else the coin with the most volume (`topOf`, kept as volume lands, ties to the incumbent), zero when nothing traded; `sweepDeadPot` pays the payer's own share to the protocol. The captain has no rights: nobody owns a ticker. Reserved official symbols cannot be invented.\n\n`MarketTickerLauncher` invents the other kind of name: **fixed inventory**. 500,000,000 at 6 decimals minted once\nat birth, opened in its own USDG pool (fee 500, spacing 10, width 30, no hook), no mint after birth, no redeem,\nno backing. It is worth what its market says. A coin under one pays a frozen 82 bps with no creator tax, and the\nlauncher reverts with `NotTheFrozenFee` on any config that says otherwise, because the factory gates only on\nregistrar and would otherwise accept whatever config id the caller passed. There is no club under a fixed-inventory\nname, so the split freezes at 60 / 0 / 40 creator / club / protocol. `QuoteRegistry.kindOf` tells the two kinds\napart by which issuer made the name, never by asking the name; FUN predates the registry and reads unclassified,\nwhich every reader treats as redeemable, which is what FUN is. `BuybackTreasuryV2` is the fee recipient from this\nrelease on, and converts a fixed-inventory name through its own market instead of forwarding it to the team whole.\n\n\n## 6. Fees\n\nLaunch fee 0.0005 ETH to the protocol at creation. Pool fee 1% base plus creator tax 0 to 2% at launch (the owner can raise that ceiling for later launches, never above 10%; TICKR carries 2%), the pool's own LP fee. `LaunchLocker.collectFees` splits the base part 50 / 10 / 40 creator / club / protocol under a redeemable name and 60 / 40 elsewhere, a fixed-inventory name included; the tax part is the creator's; the coin side splits the same way, the creator's share and the tax to the escrow in the coin, the protocol's and the club's shares burned to `0xdEaD`; everything for the creator and the protocol lands in `FeeEscrow`, pull only. See [06](./06-fees.md).\n\n## 7. Contracts\n\n`Factory` (launches, records, owner terms, CTO timelock), `LaunchDeployer` (CREATE2 coins), `LaunchSeeder` (pools, positions, exact-in swaps), `LaunchLocker` (positions and fee collection, no withdrawal), `FeeEscrow`, `TickerLauncher`, `ManagedTickerToken` and `ManagedTickerHook`, `StockQuoteLauncher`, `CoinQuoteLauncher`, `MarketQuoteLauncher`, `LaunchAndBuyRouter`, `ZapRouter`, `AnchorRegistry`, `BuybackVault`, `BuybackTreasury` (the protocol's fee recipient: collects, half funds the team and half buys and burns TICKR to start, a burn share the factory owner can only raise after a three-day delay, no owner of its own), `Token` (with the five-second snipe tax). Deployment wiring: the treasury is deployed first with the factory's predicted address; the factory address is predicted five creates ahead and checked with `require`; the seeder is predicted for the guard hook the same way; both hooks are CREATE2 with mined flag bits.\n\n## 8. Integration\n\nSee [15](./15-launching-from-code.md), [16](./16-trading-from-code.md), [17](./17-reading-state.md) and [18](./18-events-and-errors.md).\n\n## 8b. Test matrix (contracts/test)\n\n`Launch.t.sol`: pool opens with the whole supply locked, opening market cap, dev buy first, both currency orders, sells, pinned economics, fee exactness, tax cap, salt namespacing. `Fees.t.sol`: quote side split 40 / 60, coin side split with the creator's share in escrow and the rest burned, creator tax, club 50 / 10 / 40, empty collection. `Ticker.t.sol`: inventing a name opens its own pool at one dollar behind the one hook, the prediction and the top-sixteenth rule, a coin above its name refused, fee exactness, one for one both ways, reserved symbols, trades at a dollar both ways and stays backed, a buy above the offer refused whole, nobody else opens or registers a pool, `ManagedTicker.t.sol`: sustained buys then a full sell, the offer grows with circulation, a sell above circulation refused, redeem pulls from the pool and the next swap rebuilds it, the three-hop route in one unlock, the two listing buys, one visit per transaction, the hook's plain flags, pro rata club, held-back collection, the captain (founder counts double while it trades, a dead founder hands the seat to the biggest coin, the seat comes back, any volume keeps it, no captain when nothing traded, shares never exceed the pot). `Quotes.t.sol`: Stock Token and coin quotes, wild quotes refused. `Zap.t.sol`: one hop, ticker route both ways, preview equals execution, bad path, expiry. `Owner.t.sol`: club swap harmless, CTO timelock, creator transfer, closing launches keeps trading, tax cap. `Buyback.t.sol`: the half-and-half split on a mixed claim and a raised burn share, unconvertible assets to the team, buy and burn, the rate limit, the tranche and impact bounds, permissionless calls, nothing leaves but two ways, the recipient frozen per launch. `Snipe.t.sol`: a stranger pays 99% in the launch second, the launcher and its fee wallet are exempt, the decay by second, a zap's quote is what its buyer keeps, sells and fee collection untaxed. `Protection.t.sol`: the launch block is for the launch's own wallets only, the 5% hold cap, the 5.5% buy cap across sells, wallets are independent, everything lifts in the fourth block, sells and transfers are never limited, the caps count net of the snipe tax. `fork/RobinhoodFork.t.sol`: the same against the canonical Uniswap v4 on Robinhood Chain state, real USDG, live v3 for NVDA. `AuditR10.t.sol`: the zap pays the buyer and settles once (caps and tax on the buyer in the window, the router never a recipient, a sell's coin never in the router), transfers cannot assemble a holding over the cap, the exempt set, the seeder's net minimum, the treasury's prior FUN and its launcher binding.\n\n## 9. Risks\n\nSee [09](./09-risks.md).\n",
};
